# Fast Inventory Expansion Tool Plan / ??????????

Generated / ????: 2026-07-07 15:15 Asia/Shanghai

## Report Self-Check / ????

???
??????????????????????????????????????????????????????????????????????????????? Roktandrazo BD ????????

English:
Before this report was generated, it was checked for bilingual Chinese-English content, Chinese explanation, English version, no sensitive information, execution results, non-executed items, risks, next steps, no unfinished work marked complete, and alignment with the Roktandrazo BD automation framing.


## Executive Summary / ????

???
???????????????????`fast_lead_discovery.py` ?? HTTP-first ? Playwright fallback????? `evidence_url` ? `evidence_snippet`?`browser_verifier.py` ??????????????????????????????????

English:
The current tools are reusable but should not be used for broad direct DB writes yet. `fast_lead_discovery.py` supports HTTP-first scanning plus Playwright fallback and can produce `evidence_url` and `evidence_snippet`; `browser_verifier.py` supports browser verification, but its legacy upgrade path does not write the new field and prints full emails, so it needs updates first.

## Required Capabilities / ????

???
???????? TN/AR/KY ???city queue?keyword queue?HTTP-first verification?browser fallback?email extraction?`evidence_snippet` extraction?A0/B2/C/Invalid ???duplicate domain ???write-safe mode?dry-run mode ? batch limit?

English:
The expansion tool must support TN/AR/KY filtering, city queue, keyword queue, HTTP-first verification, browser fallback, email extraction, `evidence_snippet` extraction, A0/B2/C/Invalid classification, duplicate-domain exclusion, write-safe mode, dry-run mode, and batch limit.

## Current Tool Status / ??????

???
`fast_lead_discovery.py` ????? `--input` ?? TN/AR/KY ???? `--from-b2` ?????????????????????`browser_verifier.py` ??? Playwright?????????????

English:
`fast_lead_discovery.py` can enforce TN/AR/KY filtering through controlled `--input`, but the current `--from-b2` path samples randomly and is not suitable as a strict state-filtered entry point. `browser_verifier.py` can run Playwright, but its DB writes and output masking need updates.

## Proposed Changes / ????

???
????????????`--states TN,AR,KY`?`--dry-run`?`--write-safe`?`--mask-output`?`--batch-limit`?`--require-official-domain`?`--write-evidence-snippet`???????? DB?????????

English:
Recommended controlled entry points: `--states TN,AR,KY`, `--dry-run`, `--write-safe`, `--mask-output`, `--batch-limit`, `--require-official-domain`, and `--write-evidence-snippet`. DB writes must be preceded by a DB backup and followed by a masked report.

## Non-Executed Items / ?????

???
??????????????? Google Maps scraping??? Google Maps ?????? A0???????????

English:
This run did not modify production scripts; did not run Google Maps scraping; did not directly upgrade Google Maps candidates to A0; and did not restore long-term automated bulk sending.

## Risks / ??

???
??????? `browser_verifier.py --batch`???????????????????? `evidence_snippet`??????? `fast_lead_discovery.py --from-b2`????? TN/AR/KY ???

English:
Running legacy `browser_verifier.py --batch` directly may print full emails and its old upgrade logic will not write `evidence_snippet`. Running `fast_lead_discovery.py --from-b2` directly may bypass TN/AR/KY filtering.

## Next Steps / ???

???
???????????? wrapper???????????????????????? 30-50 ? dry-run?

English:
Next, add a small patch or wrapper to enforce state filtering, masked output, and new evidence-field writes before scaling to a 30-50 record dry-run.
