# 2026-08-13

## BD Inventory 自动化 (05:59–06:43 CST)
- 运行 `python bd_orchestrator.py --stage inventory --live`，44 分钟完成，job_run 状态 `partial`（stop_reason=max_loops_reached）。
- 结果：**A0 = 0/30，gap 30**，零新增（第 8 个连续零摄入日）。
- Lane A discovery：`configuration_blocked`（provider=google_places，provider_not_configured）；Lane B/C/D staging 空。
- Website Recovery：35 候选 × 5 loop → 0 封邮箱；末轮 no_email=26、network_err=9（7 个真实 network_retry_pending + 2 个 skip_platform 被误计入聚合计数）。
- 无 SMTP（send_log 今日 +0）、无 Final Send Plan、无 Send Authorization；锁已释放。
- 刷新 Ops Center 仪表盘（bd_dashboard_v3.2.py → output/bd_operations_dashboard.html）：A0=0、Manual Review=295。

## 根因（持续未变）
- Google Places 提供商未配置（`WORKBUDDY_DISCOVERY_PROVIDER` 未设 + 无凭据），自 7/29 起零摄入。
- Website Recovery 池完全枯竭（全部 no-email/network-error/platform）。
- 已修复的点：recurring previously_sent 邮箱现被 `NOT EXISTS manual_email_submission submitted_by='inventory_lane'` 排除，不再反复扫。

## 发现的新 gap（待 Ian 决策）
1. **fb_enrichment_queue 有 20 行未处理**——inventory stage 未接入 FB 队列富化。
2. **orchestrator 引用的仪表盘模块缺失**：`bd_operations_dashboard.py` / `dashboard.py` 不存在，仅 `bd_dashboard_v3.2.py` 可用（本次用它手动刷新）。
3. **聚合计数小 bug**：`skip_platform` 落入 else 分支被计入 network_err（不影响按 lead 的分类正确性，per-lead 仍正确标注 network_retry_pending）。
4. **自动化触发时区偏差**：rrule BYHOUR=15 实际在 ~06:00 CST 触发（05:59），非 15:00。

## BD Daily Collection Report (11:27 CST, automation-1785749958394)
- 只读快照，无 SMTP、无 Final Send Plan、send_log 未变（451）。
- Final Sendable Unsent = **50**（Broad 50 + Strict A0 0），Gap to 30 = 0 / Gap to 60 = 10，库存 LOW。
- 相比 08-12：Total Leads 738→761（+23，全部来自 TN+5 / AR+9 / KY+9，08-12 下午 inventory 摄入）；manual_review_needed 217→236（+19）；website_recovery 74→88（+14）；email_missing 192→220（+28）。
- 今日（08-13）收集活动 = 0（new_orgs/website/fb/manual 全 0）。
- 采集队列：Lexington KY 排首位（retail_city_queue QUEUED priority 21），lead_discovery_query_state 188 pending / 3 configuration_blocked / 0 in_progress —— discovery 仍被 provider 阻塞未执行。
- 安全校验全绿：SMTP_enabled=0、send_log_today=0、send_plan_today=0、final_send_plan_today=0（final_send_plan 表累计 132 但今日 0）。
- Ops Center 8765 仍无监听（port not listening），改以 bd_dashboard_v3.2.py 刷新 output/bd_operations_dashboard.html。

## BD Pre-Send 自动化 (12:26 CST, automation-1785804406748)
- **结果：NO_BATCH_TODAY** —— 无 21:10 CST frozen 快照（`frozen_new_outreach_20260813_et1000.json` 在 output/ 和 data/ 均不存在），第 7 个连续无快照工作日（08-05 ~ 08-13）。
- 已设置 `preflight_status = no_batch`；无 SMTP、follow-up=0、Final Send Plan 今日 0 条。
- FYI：ALLOWED_STATES 内 raw eligible unsent = **33**（较 08-12 的 7 回升，来自 08-12 下午 inventory 摄入 TN/AR/KY +23）。
- 报告：`output/pre_send_report_2026-08-13_2130.md`；脚本 `_pre_send_plan_20260813.py`。
- **上游阻塞未变**：21:10 freeze 步骤自 08-04 起未产出快照；inventory 侧 Final Sendable Unsent=50（Broad 50 + A0 0）但 freeze 层未接入，pre-send 无从取数。

## BD Inventory 自动化 (15:01 CST, automation-1784775229336)
- 命令 `python bd_orchestrator.py --stage inventory --live`。**后台任务在 loop 3/5 被杀**（约 15:30，候选 #26 Kindness & Joy Toys 网络挂起，无 traceback），留下僵尸锁 + job_runs='running'，已手动 cleanup（release_run_lock + finish_job_run=partial）。
- 结果：**Strict A0=0，Broad Ready(TN/AR/KY)=6，target 30，gap 24**（未达标，第 9 个连续零摄入日）。
- Lane A discovery: configuration_blocked（google_places 无 API key）。staging: empty。Website Recovery: 3 loops × 35 → 0 emails（每轮 no_email=26, network_err=9：7 个 network_retry_pending + 2 skip_platform）。
- Review Center auto-classify：102 条带邮箱 manual_review 跑 evaluate_a0 → 0 过 A0 → 0 approve。
- FB queue：20 行，仅 3 条 actionable（NC×2+OR×1，全 CONTACT_FORM_ONLY），需浏览器 worker 未接入。
- 无 SMTP / Final Send Plan / Send Authorization。仪表盘用 bd_dashboard_v3.2.py 刷新。
- **指标口径分歧**：日报用 `send_eligibility` 持久列算 "Final Sendable Unsent=50"（含 2 条 bounced、且几乎全为非 TN/AR/KY 州，是过期快照）；LIVE `is_broad_outreach_ready` 门禁 = 全州 57（51 manual_review + 3 new + 2 failed + 1 delivery_issue）/ TN/AR/KY 主池 6。orchestrator 权威口径为 TN/AR/KY 主池 6。
- **根因未变**：discovery provider 未配置（需人决策配置 google_places/serpapi/browser 凭证），7-29 起零新摄入。

## BD Pre-Send 自动化 (21:30 CST, automation-1785804406748)
- **结果：NO_BATCH_TODAY** —— 本批次 `new_outreach_20260813_et1000` 无 21:10 frozen 快照（output/、data/ 均不存在），第 7 个连续无快照工作日。
- 已设 `preflight_status=no_batch`、`preflight_verdict=NO_BATCH_TODAY`；无 SMTP、follow-up=0。
- FYI：ALLOWED_STATES raw eligible unsent = **36**（较 12:26 的 33 微升）；send_log 今日 0。
- ⚠️ 口径提示：final_send_plan 今日 created_at=2026-08-13 共 **30 条**，但非本批次（20 条 `p1_2_first20_20260813` planned + 10 条 superseded，来自 P1 supervised 流程）；本 pre-send 批次 `outreach_batch_date='20260813'` 实为 0 条 —— 无快照故未创建任何计划。
- 报告：`output/pre_send_report_2026-08-13_2130.md`；脚本 `_pre_send_plan_20260813.py`。
- 上游阻塞未变：21:10 freeze 步骤自 08-04 起未产出快照。

## BD Daily Collection Report (20:28 CST evening, automation-1785749958394)
- 只读快照，无 SMTP、无新建 Final Send Plan、send_log 未变（451）。
- **Total Leads 761→1017（+256）**，下午 ~16:43-16:44 CST 大批量摄入。new_orgs_today=268（早间报告误报 0，见下）。
- Final Sendable Unsent = 50（Broad 50 + A0 0）不变；Gap to 30=0 / Gap to 60=10；库存 LOW。
- **⚠️ final_send_plan_today = 30**（20 'planned' 活跃 + 10 'superseded'），非 0 —— 早间为 0。由 P1 supervised 流程（plan_p1_supervised_20260813，09:15 CST）创建 20 条活跃 'planned'，尚未发送（SMTP=0）。本次只读运行未创建任何计划。
- **发现队列扩张超 ALLOWED_STATES**：city queue front = Atlanta GA / Birmingham AL / Richmond VA（priority 30）；268 新线索遍布 GA/FL/IL/VA/MO/OK/LA/AL/SC/MS + OH(+43)/NC(+25)。manual_review_needed 236→487、email_missing 220→471（+251 与 268 新线索相关）。
- **修 bug**：`new_orgs_today` 因 collected_at 用 ISO 'T' 格式（2026-08-13T16:44:24+08:00）而脚本用 naive 空格格式区间 → 恒 0。已将 4 个 "today" 查询改为 `date(<col>) = ?`，现正确 =268。
- Ops Center 8765 仍无监听，用 bd_dashboard_v3.2.py 刷新 output/bd_operations_dashboard.html。

## BD Production Final Preflight (21:50 CST, automation-1785804413719)
- **结果：BLOCKED** —— 2 PASS / 6 FAIL / 2 N/A。第 8 个连续工作日阻断。
- FAIL：step2 无 plan(0)、step3 无 auth(0)、step4 auth/plan 不匹配(双0)、step8 Ops Center 8765 拒绝连接、step9 poller 心跳 270min 陈旧（17:16 停摆，阈值 2min）、step10 遗留 20 条 `p1_2_first20_20260813` planned 行（auth 已 superseded 但 plan 未翻转）。
- PASS：batch_id `new_outreach_20260813_et1000`；模板 SHA retail=ccb51505 / custom=5893dbc9（重算自 bd_template）。
- N/A：step5/6 组织/邮箱重复（无 plan 行）。
- 动作：写 `output/preflight_result.json`（`_preflight_20260813.py` 只读）；system_config 设 preflight_status=failed / verdict=BLOCKED / 6 blockers；SMTP_enabled='0' 未改。
- 根因未变：21:10 freeze 快照自 08-04 起未产出（第 8 天）；A0 池 6；poller ~17:16 停摆需重启 watchdog。
