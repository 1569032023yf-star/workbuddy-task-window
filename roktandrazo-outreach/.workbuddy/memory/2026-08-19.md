# 2026-08-19 工作日志

## BD Daily Results 09:00 自动化（只读汇总，昨日=08-18）
- 运行 `_daily_results_readonly.py`：清 bd_ops_api 缓存 → get_today_stats/get_inventory → 昨日批次 → 逐条 → 静态 dashboard 重生成。
- **修脚本 BUG**：`_daily_results_readonly.py` 第 ~197 行 `sqlite3.Row` 用 `.get()` 报 AttributeError → 改方括号取值，重跑通过。
- **昨日(08-18)结果**：计划 11 条（1 sent + 9 仍 planned + 1 cancelled）；SMTP 实发仅 1 封 = Game Cafe（Independence MO, lead 1055, tom@playgamecafe.com）09:21 CST，无打开信号。
- **🔴 9 条线索卡在 planned 未发送**（Hard Knox Games/ Dragon Star Hobbies/ Fantasy Factory/ Atlantis Comics/ Bookery Fantasy/ Chicagoland Games/ Blue Ox Games/ Bald Man Games/ Fallout Comics，州漂移 KY/GA/VA/OH/IL/NC/AR/FL）。
- 打开信号 0（仅 4/449 有 tracking_message_id 联动）；回复 0；hard bounce 0（08-18 08:35 有 3 条 unresolved bounce 来自 P1.2 陈旧 IMAP 扫描）；退订 0。
- 库存：1043 total；Broad Ready 76 orgs/76 locs；Strict A0=0（~15 天）；人工待审 513(rs)/467(st)/252(api)；prev_sent 448；suppressed 46。
- 采集：Nashville TN 活跃零售市；active_discovery_state=NY（08-18 更新，P17 NY 批次）；主池 TN/AR/KY。
- review_server :8765 在线，/api/ops/today & /api/ops/inventory 实时返回。
- 交付物：output/auto_report_2026-08-19.{md,json}、output/bd_operations_dashboard.html（重生成）。

## BD Inventory 15:00 自动化（live）
- 运行 `bd_orchestrator.py --stage inventory --live`（WORKBUDDY_DISCOVERY_PROVIDER=web_directory），1h36m30s 完成 clean，partial(max_loops_reached)。
- 编排器口径：BroadReady=11/30、A0=0、gap 19。
- **🔴 定位 LIMIT-31 计数 BUG**：`_count_broad_ready_pool(31)`=11 但全量 `(1000)`=64。LIMIT 落在候选查询（非 broad-ready 结果），只采样 rowid 前 31 条 → 严重低估。真实 BroadReady=64（已 ≥30 目标）。
- 状态分布：64 = 14 允许州（TN1/AR1/KY3/OR3/NC2/IN2/MN1/CO1）+ 50 非允许州（WI7/WA7/UT6/CA4/TX4/PA4 等）。
- Lane A 发现被阻断（新根因）：active_discovery_state=NY，但 web_directory 无 NY 目录页 → "no directory page for state NY"。
- 网站恢复：5 循环×35 候选 → 0 邮箱（每循环 8 network_retry_pending + 2 skip_platform；新增 Aerobellum/Board Royale 各 ~5min 超时拖慢至 96min）。
- Review 自动分类（只读）：14 条 → 0 A0 pass；FB 队列 20 条全 terminal 0 可操作。
- 无 SMTP（inventory 窗口 0 条 send_log 写入，已验证）；锁已释放；dashboard 已刷新。

## BD Pre-Send 21:30 自动化（NO SMTP）
- 运行 `_pre_send_plan_20260819.py`：查今日冻结快照 `frozen_new_outreach_20260819_et1000.json` → NOT FOUND（output/ 与 data/ 均无）。
- **结果：NO_BATCH_TODAY**，第 11 个连续工作日无 21:10 冻结快照（自 08-04 后无快照）。
- 设 system_config `preflight_status=no_batch`、`preflight_verdict=NO_BATCH_TODAY`、`preflight_follow_up=0`；无 SMTP、follow-up=0。
- FYI：允许州 raw eligible unsent=38（today_sent=0、today_plan=0）。
- 交付物：output/pre_send_report_2026-08-19_2130.md。

## BD Production Final Preflight 21:50 自动化（read-only, NO SMTP）
- 运行 `_preflight_20260819.py`（SQLite mode=ro），批次 `new_outreach_20260819_et1000`。
- **Verdict: BLOCKED** — 4 PASS / 4 FAIL / 2 N/A。
- FAIL(4)：step2 NO_PLAN（canonical 批次 0 条）；step3 NO_AUTH；step4 AUTH_PLAN_MISMATCH；step10 ACTIVE_LEFTOVER（9 planned 落在纯日期 `2026-08-19` + 1 条 stale approved auth `auth_p1_2_first20_20260813` 过期 08-14 未翻转）。
- PASS(4)：batch_id、模板 SHA（ccb51505/5893dbc9）、Ops Center HTTP 200（已恢复）、poller heartbeat 0.0min 新鲜。
- 根因同 08-18：今日 9 planned + 1 cancelled 写在纯日期而非 canonical 批次，无冻结快照、无授权。tonight_pending_20260819.json 缺失。
- A0 pool 29；frozen 快照最后=08-04。poller delivery_guard 组件死（软提示）。
- 未写 system_config（守则：预检不写 system_config）；SMTP_enabled='0' 未动。
- 交付物：output/preflight_result.json。
