# 2026-07-23 Daily Log

## 生产调度恢复 — 架构变更

- **调度权威切换**：从 Windows Task Scheduler → WorkBuddy Automation（Windows 安全策略阻止 Task Scheduler 安装）
- **旧架构**：单一 daily_operator_auto.py 驱动（automation-1782800192924），五阶段串联
- **新架构**：拆分为 5 个独立自动化，每个对应一个阶段

### 五阶段自动化列表
| 时间 | 名称 | ID | 
|------|------|-----|
| 08:30 | BD Morning | automation-1784775215334 |
| 09:00 | BD Outreach | automation-1782800192924 |
| 13:10 | BD Post-Send | automation-1784775222119 |
| 15:00 | BD Inventory | automation-1784775229336 |
| 17:30 | BD End-of-Day | automation-1784775236108 |

### 当前状态
- 今日 New Outreach: 6/20 underfilled（非 9/20；send_log 11 行含 5 重复行 bug）
- Follow-up: 0/5（明天 09:00 首次触发），Final Sendable: 110
- **A0 实际可用 = 0**：DB 中 5 条 A0 全部在 suppression_list；之前报告 "A0=1" 不准确
- Inventory: 运行中，send_enabled=false；新建 inventory_monitor_executor.py 自主执行
- 风险门: clear, manual_pause: false
- 5 个 JSON 状态文件已生成

### 注意事项
- Inventory SSL 超时需关注
- 发送窗口 09:00-13:00，15:00 Inventory 不补今日缺口
- 旧 automation-1782369770937 已 ARCHIVED/PAUSED（之前就废弃了）

### Bug 修复记录
- `sqlite3.Row` 不支持 `.get()` → 改用 bracket access `row['col']`
- leads 表无 `reviewed_at` 列 → 改用 `last_checked_at`
- `status='reviewed'` 无效 → mark_checked 只更新 timestamp 不改 status
- Lane "Review Recovery" 依赖不存在的 `reviewed` status → 移除该 Lane
- `get_db(readonly=True)` Windows URI 路径问题 → normalized forward slashes

## 城市线索采集链路专项验收 (16:06)

- 基�� CODEX_HANDOFF_CITY_OUTREACH_40.md 进行静态全链路验��
- 报告: VERIFICATION_CITY_LEAD_COLLECTION_20260723.md

### 核心结论
1. **无网络搜索能力**: stage_inventory() 仅处理 DB 中已有候选（B2/manual_review_needed + contact_form_pool），通过 httpx fetch 已知官网提取邮箱
2. **无地图搜索**: 无 Google Maps/Places API，inventory_recovery_daemon.auto_search_new_candidates() 是空桩
3. **20 query family 为僵尸配置**: search_queries() 生成字符串但无搜索执行器消费
4. **分页/游标无法恢复**: checkpoint() 函数存在但 stage_inventory() 不调用，page_cursor/active_query_family 永为 NULL
5. **city queue implemented, source discovery not implemented**
6. **P0 绕过入口**: new_lead_factory.py, db.py, recovery_replay.py, push_to_30.py, scale_*.py, brand_candidate_importer.py 直接 INSERT INTO leads，绕过 history_crosscheck
7. **沙箱限制**: 无法执行 Python/Bash/PS，动态 Nashville 测试未完成；_test_nashville.py 已准备
