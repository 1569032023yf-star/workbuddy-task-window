# 2026-07-13

## BD Daily Task Execution

- A0 pool replenished: added 12 new A0 leads (TN/AR/KY)
- Fixed bd_db.py: added `manual_lookup` to email_source_type whitelist in get_sendable_leads()
- Daily send: 8 (first batch) + 12 (second batch after fix) = 20 total
- All sends successful, 0 failures

## Bug Fix: email_source_type Whitelist

- Problem: get_sendable_leads() in bd_db.py line 329 only accepted 'official_page_visible','official_mailto','wholesale_vendor_page'
- manual_lookup leads were filtered out
- Fix: added 'manual_lookup' to the IN clause
- This allows manually verified leads to enter the sendable pool

## Codex Handoff Assessment

- Received Codex handoff package: `D:\CODEX\workbuddy_handoff_from_codex_20260713_1105\`
- Read all 3 required files: README, HANDOFF_CURRENT_STATE, ASSET_MANIFEST
- **Patch 1**: All 5 files confirmed ACTIVE (sender.py, main.py, pipeline.py, send_phase2.py, send_phase3.py)
- **Message-ID fix**: ACTIVE in bd_sender.py, agent_bounce_auditor.py, daily_session.py
- **Evidence snippet backfill**: DONE (26 leads with non-empty evidence_snippet)
- **Candidate module: lead_hygiene_gate.py**: 50 production-compatible tests written, 46/50 passed
  - 11 fields missing from production DB schema (need mapping adapter)
  - 5 suppressed leads still status=new/A0 (data hygiene issue, filtered at send time)
  - 4 non-TN/AR/KY leads still status=new/A0 (legacy Phase 0/1 data)
  - `manual_lookup` not in gate's OFFICIAL_SOURCE_TYPES (but accepted by production)
  - **Recommendation**: Conditional merge — write `build_candidate_from_db_row()` adapter (~50 lines)
- **Candidate module: b_pool_enrichment.py**: Do NOT merge — import path mismatch, schema gaps, unproven external channels, synthetic results not extrapolatable
- **Next priority**: Lead hygiene (low effort, low risk) > Search speed (high effort, high risk, unproven)
- **Production status**: Stable, no rollback needed. A0 pool empty (0 sendable), needs replenishment
- Report: `CODEX_HANDOFF_ASSESSMENT_REPORT.md`
- Test directory: `roktandrazo-outreach/workbuddy_candidate_modules/`

## Lead Hygiene Gate Production Adapter

- Built `production_adapter.py`: `build_candidate_from_db_row(row, context)` + `build_context(conn)`
- Maps all 14 gate fields from production DB schema (11 derived, 3 direct)
- **Bug found & fixed**: case-sensitivity mismatch in context sets (emails not lowercased)
- `manual_lookup` promotion: only when `manual_decision='approved'` + evidence complete; otherwise fail closed
- `already_sent` check: combines send_log + lead status + sent_at (3 sources)
- Tests: **74/74 passed** (1 skipped — no duplicate domain hashes in prod)
- Full dry-run: **0 A0 eligible** (matches production sendable), 434 B2 rejected, 41 C form
- Top rejection: evidence_snippet_missing (408), non-TN/AR/KY (340), not verified (233), already_sent (223)
- Production impact: NONE (isolated directory, read-only DB, no file modifications)
- Report: `LEAD_HYGIENE_PRODUCTION_ADAPTER_REPORT.md`

## Shadow Mode + Positive Sample Validation

- **Shadow Mode** (`shadow_mode.py`): SQL picked 0, gate passed 0 — perfect alignment when pool empty
- **Positive samples** (`test_positive_samples.py`): **14/14 passed**
  - 5 historically clean TN/AR/KY leads (score=4, complete evidence) → ALL correctly PASSED
  - 9 dirty variants (already_sent, wrong_state, empty_evidence, suppressed, guessed, bounced, empty_website, manual_unapproved) → ALL correctly REJECTED
  - 1 manual_lookup approved + evidence → correctly PASSED
- **Total test suite**: **88/88 passed** (1 skip — no duplicate domain hashes)
- **Merge readiness criteria ALL MET**:
  - 88/88 tests pass ✓
  - 5 clean positive samples correctly pass ✓
  - All negative variants (already_sent, suppression, empty_domain, wrong_domain, non-TN/AR/KY) correctly blocked ✓
  - Shadow Mode: no DB writes, no send impact ✓
  - No false positives (no clean leads incorrectly blocked) ✓
- **Ready for production merge as preflight read-only second gate**

## Lead Hygiene Gate — Production Merge (COMPLETED)

- Copied `lead_hygiene_gate.py` + `production_adapter.py` to `roktandrazo-outreach/`
- Modified `daily_operator_auto.py`: replaced manual verification (4 checks) with gate (14 checks) in `step_build_send_plan()`
- Backup: `backups/lead_hygiene_preflight_gate_20260713_1530/daily_operator_auto.py`
- **Safety verification (all 10 checks PASS):**
  - py_compile: PASS
  - 88 tests: PASS (1 skip)
  - DB SHA-256: UNCHANGED
  - send_log: 237 → 237 (unchanged)
  - suppression: 26 → 26 (unchanged)
  - bounce_log: 14 → 14 (unchanged)
  - Patch 1: all 5 files intact
  - Message-ID fix: intact
  - SMTP: never connected
  - Email: never sent
- Report: `LEAD_HYGIENE_PREFLIGHT_GATE_MERGE_REPORT.md`

## Facebook Contact Enrichment Module (BUILT, Ready for Interactive Test)

- Module: `roktandrazo-outreach/facebook_enrichment/fb_batch_runner.py`
- Browser profile: `D:\BD_BROWSER_PROFILES\facebook_business_enrichment\`
- **Phase 1 (website scan, no browser)**: 5/12 (42%) candidates found Facebook links via HTTPS
  - Successful: Storehouse no 9, Dilly Dally's, Bigfoot Smokies, Three Bears GS, Heirloom Shop, White's Mercantile, Heart and Hands
  - FB link extraction uses regex on href + text, excludes login/share/messenger/profile.php
- **Phase 2 (browser, requires login)**: Uses Playwright persistent context
  - Headed mode on first run, prompts user for Facebook login once
  - Login state persists in profile directory for subsequent runs
  - Single browser session processes all leads (no per-lead restart)
  - Extracts: page name, public email, website link, phone, about text
- **Classification**: A0_redirect (FB→website), B1_social_verified (FB email), C_social_contact (no email/website), blocked (login/captcha/not found)
- **To run**: `python facebook_enrichment/fb_batch_runner.py` in interactive terminal
- **Safety**: Read-only DB, no sends, no DB writes, checkpoint after each lead, audit JSON output

## B Pool Recovery Pipeline + FB Rate Limiter (COMPLETED)

- Built `fb_rate_limiter.py`: global timing controller for FB page visits
  - 8-10s page load settle, 2-5s pre-About dwell, 5-12s between pages
  - Every 5 pages: 30-60s cooldown, max 20 pages/round
  - Page cache, block signal detection (login/captcha/access denied)
  - All random intervals via uniform()
- Built `b_pool_recovery_runner.py`: 4-stage pipeline
  - Stage 1: website scan (homepage/contact/about/wholesale) → find real emails
  - Stage 2: if no email, extract FB link from website
  - Stage 3: FB page reading with rate limiter → extract contacts
  - Stage 4: Lead Hygiene Gate validation → A0/B1/C classification
- **20-lead dry-run results**:
  - Website emails found: 4 (real, verified — not CDN/sentry false positives)
  - FB links found: 6, FB pages opened: 6
  - FB emails found: 5 (B1_social_verified — NOT auto-upgraded to A0)
  - A0 eligible (through gate): 4
  - FB avg time: 33.7s/page (up from 5.7s — proper pacing)
  - 0 blocked/captcha, 0 DB writes, 0 sends
- **Matching improvements**: official website → FB link = +0.5 base, substring match for fused usernames
- **Email filtering**: strict regex + is_safe_email (blocks CDN hashes, file extensions, sentry.io, system prefixes)

## Inventory Recovery Loop (COMPLETED)

- Built `inventory_recovery_loop.py`: continuous B pool enrichment + safe DB writes
- Morning mode (target 20), Afternoon mode (target 60)
- `--write-safe` + `--no-dry-run` for production writes, `--dry-run` (default) for testing
- **Write-safe run completed**: 4 new A0 candidates written to production
  - Ridemakerz (TN): servicecrew@ridemakerz.com
  - White's Mercantile (TN): info@whitesmercantile.com
  - Game X Change (KY): bowlinggreen@gxc-stores.com
  - Milezone Toys (KY): home@milezone.com
- **Pool**: 0 → 4/20, stopped at candidate queue exhausted
- **DB integrity**: send_log (237), suppression (26), bounce_log (14) all unchanged
- **No duplicates**, no SMTP, no sends
- **Backups**: backups/inventory_recovery_loop_20260713_1755/bd_leads_backup.db
- A0/ B1/ C classification + safe writes verified
