# 2026-08-19 工作日志

## ONE-TIME LIVE RECOVERY SEND（09:11–09:30，Ian 授权真实补发测试）

### 执行链（全部 canonical，未改代码/automation/sender）
- **PRE**: 权威 V2 扫描 = 33 unsent 全 mx=ok；9 条 planned FSP（174,176–183）对应 lead 全部 V2 re-PASS（fresh gate 实时验证）
- **第 10 条**: 从剩余 V2 选 lead 1069 Blue Bridge Games（store-domain 邮箱 info@bluebridgegames.com，ET，official_page_visible）→ V2 PASS → 新建 FSP 184（batch=2026-08-18）
- **dry-run**: 10/10 全过 `_load_and_recheck_plan_lead` + `_new_outreach_gate`（0 skipped/failed）；authorization shadow 确认 10 条全为真实 FSP id
- **LIVE**: `daily_session.execute_final_send_plan('2026-08-18', dry_run=False, send_window_override=True)` → Fresh Authorization（id=29, approved_entry_count=10, preflight_status=passed, consumed）→ send_one 逐条（间隔 45–85s）→ **10/10 SMTP Accepted**（send_log 556–565，全部 status='sent' + smtp_accepted_at + message_id 齐全）
- send_log: 556–565（lead 727/814/815/839/883/923/1018/1053/1054/1069）
- FSP 174,176–184 全部 status='sent'
- **IMMEDIATE RECOVERY**: bounce_pipeline --once（IMAP DSN 即时扫描：scanned 15 matched 15 domain_invalid 12 unmatched 0）→ **本批 10 邮箱 bounce_log=0 / unmatched_dsn=0 / reply=0 / suppression 未新增 / duplicate send=0**（每邮箱 send_log 恰好 1 条）
- **SEND_WINDOW_OVERRIDE**: 仅本次调用参数（非持久配置），无残留 override → TIMING_TEST_VALID=false（非 recipient local 10:00，仅证明发送闭环）
- **Delayed DSN**: 复用既有 automation-1786002601925（BD Result Recovery Sync 08:45 ACTIVE），未新建

### 关键事实
- 本次是项目第 4 个真实发送日（8/5、8/14、8/18 canary、8/19 十条），**第一次 10 封连发全 SMTP Accepted 0 退信**（即时口径）
- 时间 09:15–09:25 上海 = recipient local 前夜/凌晨（ET 8/18 21:15–21:25），故 TIMING_TEST_VALID=false
- 与 P1.7E 结论一致：发送权威 = Windows Task Scheduler（09:00 触发 daily_operator_auto --live），但其窗口常量 23:00–24:00 与 09:00 触发错配 → 自动发送从未生效；本次为 Ian 授权的手动 canonical 补发
- V2 库存发送后 = 33 - 10 = 23 unsent（待下次扫描确认）

### 明日待办
- 08:45 Result Recovery Sync（automation-1786002601925）自动扫 delayed DSN / reply
- 观察 10 封后续退信/回复，验证 E2E recovery 闭环

## P1.7E REAL DELAYED BOUNCE RECOVERY TEST（11:40，生产 recovery 实测）
- 用现有 bounce_pipeline --once（唯一 IMAP→bounce_log 入口）实时扫描，**系统自动捕获 2 封本批退信**（无需 Ian 提供邮箱、无人工 insert）：
  - **Bounce 1**：Fallout Comics info@falloutcomics.com（send_log 564 / FSP 183 / lead 1054）— DSN 原文 `Name service error for name=falloutcomics.com type=MX: Host not found` → **domain_invalid**；detected 09:39（发后 15 分钟）
  - **Bounce 2**：Fantasy Factory orders@fantasyfactory.com（send_log 558 / FSP 177 / lead 815）— DSN 原文 `554 5.7.1 Message refused by DNSBL check. zen.spamhaus.org / 27.38.159.120` → **policy_rejection（发件 IP 被 Spamhaus DNSBL 拉黑，非邮箱问题）**；detected 10:00（发后 42 分钟）
- **MX_CONTRADICTION = true（仅 Fallout）**：V2 扫描时 MX Worker 报 ok，但腾讯 DSN 说 Host not found；现测 falloutcomics.com MX 无记录（A 记录在 160.153.0.27）→ Worker 当时误报或 MX 记录变化；fantasyfactory.com MX 正常（205.144.217.202 / mail.fantasyfactory.com）
- **数据库闭环（全自动）**：bounce_log +2（id 56/57）；leads 1054 status='bounced' + email_sendable=0 + contact_recovery（domain_invalid 规则）；leads 815 delivery_policy_review=1（policy 规则，正确不 suppression）；suppression +1（info@falloutcomics.com，bd_ops_poller auto_bounce_monitor）；unmatched_dsn +2（37/38，matched_send_log_id 正确）；send_log/FSP 保持 sent（reconciliation 正确）
- **policy_rejection 归因正确**：554 DNSBL 是发件 IP 信誉问题 → 不 suppression、只标 delivery_policy_review，避免误杀正常域名
- 本批 REAL_BOUNCE_RATE = 2/10 = 20% → **达到暂停阈值（≥2/10 暂停扩大）**，下一步先查 DNSBL 拉黑原因

## P1.7F TWO-BOUNCE ROOT CAUSE AUDIT（13:24，只读）
- **Fallout Comics（lead 1054）根因 = WORKER_FALSE_POSITIVE**：V2 扫描（8/18 权威 scan + 8/19 09:14 重扫）MX Worker 均报 ok，但腾讯 DSN（09:39）Host not found；实时测 falloutcomics.com **无 MX 记录**（NS=GoDaddy domaincontrol、SPF=Sendinblue、A=160.153.0.27）→ 域名是"建站托管无企业邮箱 MX"类型，Worker 误报 ok（false positive）；无 mx_cache 条目（STALE_CACHE=false）、无 fallback（官方路径）
- **Fantasy Factory（lead 815）根因 = SENDER_REPUTATION**：DSN `554 5.7.1 DNSBL check. zen.spamhaus.org / 27.38.159.120`；27.38.159.120 = APNIC 亚太段，与腾讯企业邮箱出站 IP 特征一致（send_log 无 IP 字段，无法 100% 证明但 DSN 由腾讯产生即该 IP 为腾讯 relay）；历史 bounce_log/unmatched_dsn **无任何 DNSBL/Spamhaus 记录**（仅 2 条旧 policy 类：odysseygames relaying denied、gamedaymiami recipient service policy，均非 DNSBL）→ 本批首例
- 分类：Fallout=RECIPIENT_DATA_QUALITY（域名无 MX 被 Worker 误判）；Fantasy=SENDER_REPUTATION（腾讯 relay IP 被 Spamhaus 拉黑）
- PRODUCTION_BUG_CONFIRMED=false（Worker 误报属已知抖动/边缘案例，非代码缺陷证据；无缓存无 fallback 使用）；CODE_CHANGE_REQUIRED=false；SEND_SCALE_SHOULD_REMAIN_PAUSED=true（2/10=20% 达暂停阈值 + DNSBL 需先观察）

## P1.7G MX WORKER FALSE POSITIVE ROOT AUDIT（14:40，只读）— ROOT CAUSE 定位
- **Worker 实现**：cloudflare/src/index.js handleMXCheck（160-212）——MX 查询 `cloudflare-dns.com/dns-query?type=MX`：
  - Status=0 且 Answer>0 → `mx_pass`
  - Status=3 → `nxdomain`
  - **else（含 NoAnswer，即域存在但无 MX）→ 查 A 记录 → A 存在 → `implicit_mail_route`**（193-199 行）
- **status map（preflight_gate.py:62）**：`"implicit_mail_route": "ok"`（注释"RFC5321 隐式投递路由，不算 fail"）
- **重放实证**：falloutcomics.com 经 canonical Worker 连续 3 次 = **ok / ok / ok**（false positive 稳定复现）；本地 DNS：MX=NoAnswer（无 MX）、A=160.153.0.27、AAAA=无
- **根本矛盾**：Worker 把"A-only 域"判定为可投递（RFC 5321 隐式 MX 语义），但**腾讯 SMTP 实际不做 A-fallback**（真实 DSN：Name service error type=MX Host not found）→ 判定策略与发件 MTA 实际行为不符
- NARROW_FIX（仅报告）：Worker index.js 191-202 的 A-fallback 分支 + preflight_gate.py:62 的 implicit_mail_route→ok 映射——应改为把无 MX（仅 A）判定为 no_mail_route / null_mx（fail），或至少 require explicit MX。未修改

## P1.7H MX A-ONLY FALSE POSITIVE NARROW FIX（15:05，完成）
- **修改 2 文件**（最小窄修，未动 V2/Preflight 架构/Sender/FSP/Discovery/Schema）：
  1. `cloudflare/src/index.js` handleMXCheck：无 MX Answer（NoAnswer）→ 一律 `no_mail_route`（fail-closed），A 记录仅审计用途，不再推导 implicit_mail_route
  2. `preflight_gate.py` `_WORKER_STATUS_MAP`：`"implicit_mail_route": "no_mail_route"`（客户端防御，旧 Worker/历史响应也不放行）
- **部署**：wrangler deploy 成功（Version fff757d2-b64d-4007-bb83-8087dde14f5f，npx 缓存 32026684e21afda6）
- **回归 8/8 稳定**：falloutcomics.com=no_mail_route(FAIL)×3 ✓；fantasyfactory.com=ok(PASS)×3 ✓；5 个真实 MX V2 域全 ok×3 ✓；NXDOMAIN=nxdomain×3 ✓
- **全量重扫**：CAMPAIGN_ELIGIBLE_V2 = **33 → 23**（差 10 = 本批已发送 10 条）；新增 blocked `mx:no_mail_route: 3`
- **A-only 影响核实**：8 个 no_mail_route 域（brooklynlab/eparks/gamesngizmos/genxcomics/goldrushgames/momastore/toptentoys/villainouslair）全部是 `guessed_email` + manual_review_needed 池（本就不进 V2）；**unsent V2 池中无 A-only 残留**；falloutcomics.com 已不在 V2（已 bounced+suppression）
- A_ONLY_REMOVED_COUNT=0（unsent V2 无 A-only 可移除；fallout 由发送+退信流程移出）

## P1.8 DAILY BD OPERATIONS MODE（15:27 进入）— browser_maps 恢复 + Ithaca smoke
- **Playwright + Chromium 已装**（隔离 venv C:/Users/15690/.workbuddy/binaries/python/envs/default；chromium_headless_shell 下载成功，launch OK）
- **Ithaca smoke test（DIRECT 模式，toy store Ithaca NY）→ FAIL**：
  - 浏览器正常打开 Google Maps 并滚动加载（203 卡片计数），但**提取 0 真实商户**（只提取到占位横幅 "Results"/"Get the most out of Google Maps / Sign in"）
  - 深度诊断：`[role="feed"]`=0、`.Nv2PK`=0、`a[href*="/maps/place/"]`=0、`aria-label*Results`=0；页面显示 **"You're seeing a limited view of Google Maps"** 横幅（仅 1 个详情面板 Alphabet Soup）
  - 直连与 Astrill 代理均同样 limited view → **Google 对匿名/无 cookie headless 会话强制降级页**（2026 新反爬策略），非 selector 过时可修
- **bl ocker**：browser_maps DIRECT 模式当前环境无法产出真实商户列表；FILE 模式仍需人工先跑 scraper 存 JSON（同样受 limited view 限制）
- **未改 scraper 代码**（SYSTEM_FREEZE；修复需持久化已登录 profile，属开发范围需 Ian 决策）
- P1.8 状态：DAILY_NEW_V2_TARGET=30 未推进（Discovery 无可用 provider：google_places 无 key、web_directory 仅三州、browser_maps 被 Google 降级）

## P1.8B OFFLINE OSM DISCOVERY FEASIBILITY（16:37，只读完成）
- **NY Geofabrik PBF 下载**：data/osm/new-york-latest.osm.pbf（495MB，36s）；pyosmium 已装 venv
- **Ithaca 20km 提取**：raw 25 → unique **24** 家（books 8 / antiques 5 / gift 4 / sports 5 / craft 1）；有 website 9、有 address 15、有 phone 10
- **DB 去重**：ALREADY_IN_DB=0、**NET_NEW=24**（DB 无 Ithaca 数据）
- **ICP 过滤后**：剔除 big-box 连锁（Barnes & Noble/Dick's/Michaels/Advantage/Finger Lakes/Cornell）→ **18 家独立零售**，其中 **5 家有官网**（Buffalo Street Books/Odyssey Bookstore/Homegrown/Pastimes/Taughannock）
- **判定**：NET_NEW(24) ≥ 15 → **OSM_DISCOVERY_FEASIBLE = true**——离线州级数据可作 NY 免费商户底库；但**有官网的独立零售仅 5/18（28%）**，实际 V2 转化率预估低（官网邮箱需后续深挖），仍优于 browser_maps（0 结果）
- 未改任何代码/schema/系统；不因测试成功自动开发 provider（待 Ian 决策是否接入）

## P1.8C OSM INGEST COMPATIBILITY AUDIT（16:57，只读）
- **DiscoveryService 最小合法入口** = `provider.search_places() → ProviderPage → results[PlaceSearchResult]` → `run_places_batch`（_upsert_result 做 normalization/dedup → _classify_and_maybe_create_lead 做 ICP/lead 创建）→ `run_staging_postprocess`（官网→邮箱→A0/manual/contact-form）→ V2/MX。**OSM 只须提供合法 ProviderPage 即走完整现有链**
- **browser_maps FILE mode 可复用**：`_load_from_json_file` 读取 JSON → 构造 PlaceSearchResult（business_name/formatted_address/city/state/phone/website 字段）→ ProviderPage；OSM 提取结果字段可 1:1 映射生成兼容 JSON，**零代码复用**
- **无通用 file provider**：mock_provider 是 Nashville 硬编码测试数据，非通用；browser_maps 是唯一带 FILE mode 的 provider
- **安全边界确认**：OSM 数据仅含 name/address/website/phone（无邮箱字段）→ 必然走官网→邮箱提取→V2 链，不可能绕过质量门直入 A0/V2
- **推荐 OPTION_2**：一次性转换脚本（OSM POI → browser_maps FILE 兼容 JSON，数据准备非系统代码）+ DISCOVERY_PROVIDER=browser_maps + BROWSER_MAPS_JSON_FILE → 现有 run_places_batch 消费。CODE_CHANGE_REQUIRED=false

## P1.8D ITHACA OSM → EXISTING DISCOVERY INGEST（18:09，现有资产接入验证）
- **JSON 生成**：24 家 OSM POI → browser_maps FILE schema（稳定 id：provider_result_id=osm:<type>:<id>，place_id=osm-<type>-<id>）；未加 email、未预判 A0/V2
- **RUN 1**（DISCOVERY_PROVIDER=browser_maps, BROWSER_MAPS_MODE=file, JSON_FILE=ithaca_bm_file.json，仅 Ithaca）：results_seen=24、**new_unique=24（≤24 ✓）**、dup=0、leads_created=14（manual_review 14 等）——无跨 query family 重复
- **Postprocess**（commit 后）：9 家官网访问 → **3 个官方邮箱**（Homegrown info@homegrownskateshop.com / Taughannock info@visitithaca.com / Pastimes contact@pastimes.com，均 official_mailto）；Advantage Sport 提取到 splidejs 垃圾邮箱（JS 库误匹配，manual_review 保护，fail-safe ✓）；9 lead 落库 1093–1101
- **V2/MX**：3 个邮箱域 MX 全 ok，但被 `timezone:UNSET + organization_key_empty` BLOCK——**预期行为**（postprocess 创建 lead 缺 tz/org_key/mx 属正常初始态，需上游 enrichment 补字段，与 1056 模式对比确认；本测试止步不补）
- **幂等**：RUN 2 new_unique=0、dup=24、leads=0 ✓ IDEMPOTENCY_PASS
- **状态机**：toy store/board game store completed，其余 18 family pending（+google_places 历史 provider_not_configured 1 条，P1.7D 遗留非本次）；**QUERY_FAMILY_DUPLICATE_LOOP=false、CITY_PREMATURELY_COMPLETED=false**
- **教训**：DiscoveryService.run_staging_postprocess 依赖调用方 commit（run_places_batch 内部 commit，postprocess 不 commit）——首次测试未 commit 导致 9 lead 回滚，重跑修复

## P1.8E POST-DISCOVERY ENRICHMENT WIRING AUDIT（18:27，只读）— 关键真相
- **TIMEZONE_WRITERS**：`timezone_resolver.py:266 apply_timezone_to_leads`（批量 UPDATE recipient_timezone/timezone_status/sendable，--apply 写库）——**存在但无任何生产调用方**（grep 全库仅 0818 临时脚本用 resolve_timezone 单条，未用此批量函数）
- **ORG_KEY_WRITERS**：**生产链无 organization_key 落库写入者**！`broad_outreach_gate._gen_organization_key`（143）是**读取时 fallback 计算**（不 UPDATE）；落库写入只存在于 `_discover_insert_0818*.py`（我的一次性临时脚本）与 `_p17b_recovery_0818.py`
- **1056–1078 补齐真相**：这 23 条 V2 的 org_key/tz/mx 是我用 `_discover_insert_0818b-e.py` 临时脚本**手动 UPDATE** 的——生产链**没有**这个 enrichment 步骤
- **orchestrator stage_inventory 官网富化 lane（467+）**：只处理 `(email IS NULL OR email='')` 的 lead（找邮箱），**不处理"已有邮箱但缺 tz/org_key"的 lead**；且不调用 apply_timezone_to_leads
- **Ithaca 3 条 blocker 根因**：staging postprocess 用 bd_db.insert_lead 创建 lead（不写 org_key/tz/mx，已知模式）→ 无自动 enrichment 接续 → timezone 有 CLI 未接线、org_key 无落库写入者 → V2 门（organization_key 非空 + tz RESOLVED）fail
- 最小恢复（仅报告）：① 复用 apply_timezone_to_leads CLI（--apply）先手动跑验证；② org_key 用 _gen_organization_key 既有逻辑补一次 UPDATE（或接入 orchestrator post-staging）

## P1.8F POST-STAGING ENRICHMENT VALIDATION（18:30，完成）
- **Timezone**：复用 `timezone_resolver.apply_timezone_to_leads(dry_run=False, state_filter='NY')` → resolved=9（NY 全部可解析），3 目标全 RESOLVED（America/New_York）
- **Org key**：严格复用 `broad_outreach_gate._gen_organization_key`（仅空时写）→ 3 条 `org:domain:<domain>`，sent_dup cross-check = 0（无历史冲突）；1094 Advantage 一并补（同机制）
- **V2 重扫（canonical Worker）**：**3/3 全 PASS**（1095 Homegrown / 1096 Taughannock / 1101 Pastimes，mx=ok、tz RESOLVED、org_key 有、blockers=[]）
- **ITHACA FUNNEL**：RAW 24 → website 9 → email 3 → **V2 3**；RAW_TO_V2_RATE = 3/24 = 12.5%
- **结论**：POST_STAGING_ENRICHMENT_CAPABILITY_VALIDATED=true —— 现有 timezone + org_key 能力完全足以把 staging 新 lead 送入 V2；只需把这两个既有函数接到 staging 后（生产接线，本轮未改 orchestrator）

## TONIGHT SEND READINESS PREP（18:31，只准备不发送）— BLOCKED
- **V2 unsent = 26**（ET10/CT8/MT1/PT7，含 P1.8F Ithaca 3 条 1095/1096/1101 已进 V2）
- **生产门全绿**：manual_pause=false、standing_authorization=true、risk_gate=clear、send_pause=false、SMTP/IMAP/MX Worker 正常、V5 template ccb51505 锁定 ✓
- **执行器仍 PAUSED**：automation-1785804421539（ExecutionHost未验证）→ EXECUTION_HOST_READY=false，今晚 22:00 不会自动飞出
- **SEND_SCALE_PAUSED = true（未解除）**：今日 8/19 批次 10 发 2 退 = **20% bounce**（Fantasy Factory 554 Spamhaus DNSBL 27.38.159.120 sender reputation + Fallout domain_invalid 已由 P1.7H 修复）；DNSBL 未做任何 delist、未确认解除 → 按 P1.7F 阈值（≥2/10 暂停）与 fail-closed 原则，**不创建今晚 FSP**
- TONIGHT_PLANNED = 0、未建 Authorization、未发送；V2 26 条保留待解除后使用

## TONIGHT FSP PREPARE ONLY（18:35，已建计划未发送）
- **V2_UNSENT = 26**（实时权威扫描）→ 选最强 10 条 → create_plan（batch=2026-08-19，canonical）→ FSP 185-194
- **⚠️ 复检发现 FSP186（lead 1069 Blue Bridge）已被今天早上发送（send_log 565）**——临时脚本 zip(ids,res) 顺序错位误选；**已 cancel FSP186**（skip_reason=recheck_failed:previously_sent_email_lead1069_send_log565），未补低质量候选
- **最终计划 = 9 条**（FSP 185,187-194）：全部复检通过（V2/mx/tz RESOLVED/verified=1/sent=0/supp=0/bounce=0，locked retail_distributor_v5_locked ccb51505）；时区 ET2/MT1/PT6
- **未创建 Authorization、未发送、未 override**；SEND_SCALE_PAUSED=true 与 EXECUTION_HOST_READY=false 保持原样（计划冻结不执行）
- 教训：选候选必须用权威 _final_v2_scan 输出，勿在临时脚本内 zip 全库 ids 与 scan 结果（顺序错位风险）
