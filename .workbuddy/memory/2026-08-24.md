# 2026-08-24 工作日志

## P2.3A — WorkBuddy Automation 治理（生产调度权收口到 Windows Task Scheduler）
只读读取两个条件项 prompt（从 workbuddy.db `automations` 表，view 工具对无前缀 ID 报 not-found）后决策：
- 1785749958394 BD Daily Collection Report 20:30 → DISABLE（仅 read-only 报告/状态快照，不写生产必需状态；EndOfDay 00:25 负责正式日报）
- 1786002601925 BD Result Recovery Sync 08:45 → **KEEP ACTIVE**（运行 result_recovery_sync.py：tracking+bounce+reply+unsubscribe 扫描+last_success_at+poller 心跳，属 delayed DSN/reply/bounce/suppression reconciliation 唯一功能，Windows PostSend/Inbox Poll 不覆盖）

停用（PAUSE，不 DELETE，记录 BEFORE_STATUS 可恢复）共 8 个：
- 1785804421539 BD Production Outreach（原已 PAUSED，标记 LEGACY_PRODUCTION_TRIGGER）
- 1785804413719 BD Production Preflight（21:50）
- 1785804406748 BD Production Pre-Send（21:30，与 Windows PreSend 22:30 重复）
- 1785720738893 / 1785720747436 Weekend Inventory AM/PM（库存统一收口 Windows Inventory-Recovery）
- 1785406176740 [ONCE] BD Daily Results 09:00（名 ONCE 实 DAILY，历史遗留）
- 1784775229336 RoktRazo BD Inventory 15:00（与 Windows 15:00 Inventory-Recovery 重复）

结果：活跃 WorkBuddy Automation 仅剩 1 个（1786002601925 Result Recovery 08:45）。
ACTIVE_WORKBUDDY_SMTP/PRESEND/INVENTORY_TRIGGER 均 = 0。Windows Tasks 未触碰（沙箱禁用 schtasks 无法 live 核验，但本轮零修改）。
CODE_CHANGED=false / DB_CHANGED=false（仅 workbuddy.db automations.status 翻转，bd_leads.db 生产数据未动）。
PRODUCTION_SCHEDULER_AUTHORITY = Windows Task Scheduler。

## P2.3B — REAL PRODUCTION PREFLIGHT HARDENING（技术安全门，非权限治理）
确认并消除了 P2.2D 遗留的 `preflight_passed=True` 硬编码假通过。
- 循环依赖确认：生产 `run_preflight` 内含 `check_auth_entries`（要求授权已存在），而 `create_send_authorization` 是写 preflight_status 的唯一入口、原被硬编码 `preflight_passed=True` 调用 → 若用完整 run_preflight 决定授权创建，会因授权不存在而恒失败。**AUTH_PREFLIGHT_CIRCULAR_DEPENDENCY=true**。
- 最小无损接线（无新 schema / 无新状态机 / 无 provisional auth）：给 `preflight_gate.run_preflight` 加 `include_auth_entries: bool=True` 参数（向后兼容）。PRE_AUTH = 除 auth_entries 外的全部检查；POST_AUTH = auth_entries（授权创建后跑一致性校验）。
- `daily_session.execute_final_send_plan` 改动：创建授权前先跑 `run_preflight(include_auth_entries=False)` 拿真实结果；pass 才 `create_send_authorization(preflight_passed=True, preflight_passed_status='passed')`；fail 则 fail-closed 不创建授权（send_one 因无 approved 授权拒绝）。dry-run 用 persist_cache=False 只读。原有硬门（V2/EXPLICIT_MX/suppression/bounce/history/dup org/manual_pause/standing_authorization/risk_gate/locked V5/timezone）全部保留。
- 回归（DB 拷贝、不发送、跑完即删）：6 场景 + 循环证明全 PASS。S1 canary 1056-1060 PASS+真实授权；S2 8/21 事件 8 lead（domain_invalid）V2 全阻；S3 stale plan FAIL；S4 old approved auth FAIL + 无授权完整 run 必 FAIL；S5 duplicate FSP FAIL；S6 stale/bad MX FAIL-CLOSED 且不建授权；CIRC_PRE_AUTH 无授权可 PASS、CIRC_FULL_RUN 无授权必 FAIL。
- CODE_CHANGED_FILES = preflight_gate.py, daily_session.py。SCHEMA_CHANGED=false, NEW_PIPELINE_CREATED=false。生产 bd_leads.db 零修改（验证 REGRESS/OLD/OTHER 产物=0）。MANUAL_PAUSE 保持 true（未动）。

## P2.3C — PRODUCTION DECISION AUTHORITY GOVERNANCE（权限政策，文档化）
仅记录权限矩阵，不改代码/DB/Automation。新建 `roktandrazo-outreach/OPERATIONS_AUTHORITY_POLICY.md`：
- LOCAL SYSTEM = Windows Task+Local Python（确定性规则，全门 PASS 自动发，不每天问 Ian）。
- WORKBUDDY = 智能富化+异常分析（非调度/非发送权），结果须重过 V2→MX→FSP→Preflight。
- IAN = 业务/风险最终决策（incident 恢复、扩量、ICP/州实验、模板核心、SMTP、阈值、删资产、付费服务、架构变更）。
- DAILY_MANUAL_SEND_CONFIRMATION_REQUIRED=false；WORKBUDDY_CAN_DIRECTLY_SEND/RELEASE_MANUAL_PAUSE/CHANGE_V2/CHANGE_SEND_SCALE 均 false；IAN_APPROVAL_REQUIRED 三项为 true；HUMAN_REVIEW_CAN_BYPASS_V2=false。

## P2.3D — POST-PREFLIGHT 5-EMAIL PRODUCTION CANARY（BLOCKED：MX 验证 Worker 不可达）
Ian 明确授权的真实 5 封生产 Canary，验证 P2.3B 修复后最终生产链。但**无法从本沙箱执行真实发送**：
- 硬门 MX 验证（`preflight_gate.query_mx` → Worker `roktandrazo-email-tracker.../internal/mx-check`）返回 `401 {"error":"unauthorized"}`（本环境无 `TRACKING_DASHBOARD_API_KEY`/`DASHBOARD_API_KEY`，回退硬编码 token 被拒）与 `403 Cloudflare error 1010`（Astrill 代理出口 IP 被 Cloudflare 国家/地区封禁）。
- 后果：`query_mx`→`dns_error`→fail-closed。PRE_AUTH preflight 的 `check_dns_freshness` 与逐条 V2 recheck（`_new_outreach_gate`→`review_campaign_eligible_v2`，live `query_mx`）**全部 MX 失败** → 0 条能过 MX 门 → 0 可发送。
- `mx_cache_*` 仅 5 条域且均为 2026-08-21（>24h stale），preflight 会重查→同样 401/403→fail。
- 诊断结论：属**环境/基础设施问题**（本沙箱缺 token + 出口被 Cloudflare 按国家封禁），非 P2.3B 代码缺陷；生产 Windows 主机（出口不同 + token 已配置）应可正常达 Worker。
- 处理（符合 P2.3C 治理：MX 异常→fail-closed + RESUME_SEND_DECISION=IAN）：**未发送、未建 FSP、未翻 manual_pause（保持 true）、未改代码/DB/Scheduler/Auth**。生产 bd_leads.db 全程只读（mode=ro）。
- 候选分析（只读，排除 8/21 事件 lead 134/136/153/275/283/284/297/300 与 1056-1060）：23 条通过 V2 全部组件**除 live MX**（V1+官方第一方来源+evidence+tz RESOLVED+未发送/未 suppression/无 hard bounce+org 未发过；无一条命中 5 个缓存 ok 域）。数据质量注意：lead 1094 的 email 字段为 `splidejs/splide-extension-auto-scr`（明显 GitHub 路径残片，非真实邮箱），应在恢复 MX 后人工复核剔除。
- 建议 Ian：从生产 Windows 主机（BD Execution Host，Worker token 已配置且 Cloudflare 放行出口）重跑本 Canary；或先修 Worker auth token / Cloudflare WAF 放行出口 IP，再执行。
- 本轮输出 KEY=VALUE：TARGET=5；CURRENT_V2_UNSENT=0（含 MX 门，因 MX 不可验）；SAFE_FSP_CANDIDATES=23（除 live MX 外全过）；PLANNED/ATTEMPTED/SMTP_ACCEPTED/FAILED/SKIPPED=0；PRE_AUTH_PASS=false；MX_PASS=false；REAL_PREFLIGHT_RESULT_USED=true（P2.3B 已去硬编码，且本次诊断证实门返回真实失败）；DAILY_SESSION_USED=false（MX 门不通，执行只会产生 failed 产物，故未跑真实链以免污染生产）；MANUAL_PAUSE_RESTORED/SEND_WINDOW_OVERRIDE_RESET=true（均未改）。

