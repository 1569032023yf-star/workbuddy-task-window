
## P2.1A INSTALL BDExecutionHost SERVICE（09:21，服务已装但无法启动）
- **安装**：通过 python.exe 提权通道执行 `bd_execution_host_service.py install` → 服务注册成功（LocalSystem / Auto）
- **启动失败诊断链**（4 轮）：
  1. 首次启动 30s 超时 → 根因①：脚本 `install_service` 用 `sc create binPath="python.exe script.py run"`，pywin32 不识别 `run` 参数（HandleCommandLine Unknown command）
  2. binPath 修正为 pywin32 标准 `pythonservice.exe <class>` → 仍 30s 超时 → 根因②：**pywin32_postinstall 从未运行**（pythonservice.exe 未复制到 python 根、pywintypes313.dll/pythoncom313.dll 未复制到 python313.dll 旁）→ 已补齐三个文件
  3. 补齐后错误变为 "**函数不正确**"（ERROR_INVALID_FUNCTION）→ 根因③：**portable managed python 的注册表环境不一致**（HKCU PythonCore\3.13 InstallPath 指向系统 python AppData\Python313，而服务用 .workbuddy\binaries\python\3.13.12）→ pythonservice.exe 定位 python home 失败
- **现状**：SERVICE_INSTALLED=true、SERVICE_RUNNING=false（"函数不正确"）；主日志未生成（SvcDoRun 未执行到）
- **判定**：pywin32 Windows Service 在 portable python 环境下的已知限制（pythonservice.exe 依赖注册表 python home）——**环境级 blocker**，非代码 bug
- **备选（未执行，待决策）**：Windows Task Scheduler 路线（P1.7E 已证 Task 存在）：把 RoktRazo-BD-Daily-Outreach 触发时间从 09:00 对齐到 23:00（或新建 stage 触发），用现有 bd_orchestrator --stage outreach 替代服务循环；或改注册表 HKCU PythonCore\3.13 InstallPath 指向 managed python（影响系统 python 关联，需谨慎）

## P2.1B TASK SCHEDULER 路线落地（09:38，用户拍板方向②）
- **决策**：用户指示停止尝试启动 BDExecutionHost，保留服务注册但不用；改走 Windows Task Scheduler
- **BDExecutionHost 服务**：Start 2→3 (Manual)（HKLM SYSTEM\CurrentControlSet\Services\BDExecutionHost，经提权 python winreg 写入）✅ 保留注册、不再自动启动、当前 STOPPED
- **发现 6 个现存 Rokt/BD Task**（提权 python 内 schtasks 可查；COM Schedule.Service 即使提权+CoInitializeSecurity 仍 E_ACCESSDENIED 0x80070005，弃用 COM，改 schtasks /xml）：
  - RoktRazo-BD-Daily-Outreach 09:00 daily_operator_auto.py --live → **已 DISABLE**（旧发送链路，与 23:00 冲突）
  - Roktandrazo_Daily_Outreach 10:00 Documents\线下bd\daily_runner.py --dry-run → **已 DISABLE**（陈旧）
  - RoktRazo-BD-Morning 08:30 orchestrator --stage status → 保留
  - RoktRazo-BD-Inbox-Morning 08:30 / PostSend-Poll 13:10 inbox 轮询 → 保留
  - RoktRazo-BD-Inventory-Recovery 15:00 → 保留
- **新建 3 个 Task（Asia/Shanghai 本地时区，ENABLED，InteractiveToken+SID，--live 生产模式，StartWhenAvailable，IgnoreNew）**：
  - RoktRazo-BD-PreSend 22:30 → bd_orchestrator.py --stage pre-send --live
  - RoktRazo-BD-Outreach 23:00 → bd_orchestrator.py --stage outreach --live
  - RoktRazo-BD-PostSend 00:10 → bd_orchestrator.py --stage post-send --live
- **dry-run 全绿**：pre-send `[DRY RUN] Final Send Plan preview only: new=40, follow-up=5`；outreach `[BLOCKED] No Final Send Plan`（fail-closed 正确，今晚 pre-send live 跑后才会有 FSP）；post-send `[DRY RUN] preview only` ✅
- **助手脚本**（保留证据）：output/_admin_tasks.py / _apply_tasks.py / _dump_tasks.py / _diag_tasks.py + 对应 _launch_*.py；提权通道 = ShellExecuteW(runas, python.exe)，subprocess 调 schtasks（WorkBuddy 沙箱禁 schtasks 但提权进程内可用）
- **待办**：SEND_SCALE_PAUSED=true 仍未解除（DNSBL 未确认）；今晚 22:30 起三个 Task 首次自动运行，仍需确认 SEND_SCALE_PAUSED 状态下 outreach 是否 fail-closed 不发

## P2.1C 收尾"未完成"尾巴（09:46，核查 8/19 冻结 + 发信前置）
- **关键纠正**：DB 中**不存在** SEND_SCALE_PAUSED 全局 key；实测 send_pause=false / risk_gate_status=clear / manual_pause=false / standing_authorization=true。所谓"暂停"是会话内决策态，未落库。
- **8/19 冻结真因**：9 条 FSP（id 185/187-194，leads 1068/1070/1071/1072/1073/1074/1075/1076/1077）status=planned，**无 send_authorization**（system_config 最新授权停在 2026-08-18，auth_by=system）+ send_log 0 条 → 从未发信。实际最后真实发信=2026-08-18（11 条 sent）。8/19 另有 1 条 cancelled（lead 1069 Blue Bridge Games，previously-sent）。
- **pre-send --live 已为 08-20 建 FSP**：strict_a0 planned 44 条（dry-run 显示 40 new + 5 follow-up）。建 FSP 不发信，验证 live 写链路 OK。
- **ops_center 心跳**：127.0.0.1:8765 REACHABLE ✅（preflight step8 满足）。
- **outreach --live 白天行为**：`[SKIP] Outside send window (23:00-23:59:30 Asia/Shanghai)` —— 发信硬约束为上海 23:00 后窗口，白天直接跳过（不建 auth、不发信）。
- **结论**：系统侧已全部就绪（Task 对齐 + 服务 Manual + 旧 Task 禁用 + 今日 FSP 已建 + ops_center 可达）；真实发信唯一缺口=① 23:00 窗口（今晚 Task 自动触发）② 用户 SMTP 授权（项目铁律：SMTP 需 --live+人工确认，且 8/19 DNSBL 554 Spamhaus 风险未消）。8/19 那 9 条仍卡 planned 待补发决策。
- **遗留决策点（待用户拍板）**：今晚 23:00 是否授权真实 --live 发信（08-20 新批次）；8/19 那 9 条冻结 FSP 是否纳入今晚补发。

## q-0 Supervised Canary Acceptance Path（P2.1F 阻塞解锁）
- **背景**：P2.1F「经 Task Scheduler 发 1 封 canary」字面不可执行（schtasks 触发被沙箱禁用 + UAC 拒；窗口无 override 钩子；pre-send 非幂等）。q-0 收回硬要求，改「新增仅用于验收的 supervised canary execution path」。
- **交付**：新建独立脚本 `roktandrazo-outreach/_canary_acceptance_path.py`（**仅此 1 个新文件，零生产代码改动**）。复用生产函数，不碰 scheduler/window/SEND_SCALE/正式批次。
- **链路（已 dry-run 全绿）**：Campaign Eligible V2 gate（lead 1056 The Wyvern's Tale，mx=ok/tier=E1/pass）→ 孤立 Final Send Plan（batch=`canary_2026-08-20`，fsp_id=283，与生产 `2026-08-20` 88 条 planned 完全隔离）→ 预热 mx_cache → 创建 Fresh Authorization（approved）→ `run_preflight`（安全门全 PASS）→ `execute_final_send_plan(send_window_override=True)` 抵达 send_one → `scan_bounce_and_reply` recovery。
- **关键排查**：V2 初判失败是测试未加载 `.env` 导致 MX Worker 用无效 fallback token（401→dns_error）；真实脚本加载 .env 后 query_mx 返回 ok。MX Worker 仅经 Astrill 代理可达（直连超时），token 取自 `TRACKING_DASHBOARD_API_KEY`。
- **q-0 输出**：CANARY_PATH_AVAILABLE=true / CANARY_SEND_ALLOWED=false(dry) / CODE_CHANGE_REQUIRED=false / FILES_CHANGED=_canary_acceptance_path.py / FULL_CHAIN_CAN_BE_TESTED_NOW=true。
- **生产侧发现（独立于 canary）**：`run_preflight` 的 `check_stale_objects` 会标记全库任何非当前批次的 planned 计划 —— 生产库自身有 `2026-08-20` 88 条 + `2026-08-19` 9 条 stale planned 行（pre-send 非幂等累积）+ 1 条旧 active auth。这意味着**生产 preflight 当前也会因 stale_objects 失败**，属生产数据卫生问题，q-0 范围内不修（不动正式批次）。
- **待办**：用户授权后再实际发 1 封 live canary（`python _canary_acceptance_path.py --live`）；生产 88 条 stale planned 行清理另议。

## FSP stale 问题处理（用户指令「处理 FSP stale 问题」）
- **根因修复（P1.9 范围内生产 bug 修复）**：`final_send_plan.create_plan` 原纯 INSERT 非幂等 → 重复 pre-send 累积重复行 + 旧批次滞留。`final_send_plan.py` 已改：INSERT 前按 `message_type` 退役全库既有 `planned` 行（`status='cancelled', skip_reason='superseded_by_new_plan'`）。使 pre-send 幂等、杜绝双发、并让 preflight stale_objects 长期干净。空 entries 时早返回不误删。
- **数据清理（一次性，--live 已执行，事务提交+integrity_check OK）**：新建 `_fsp_stale_cleanup.py`（默认 dry-run，--live 才写，写前自动备份+SHA256+回滚）。
  - 去重 `2026-08-20`：删 44 重复行 → 留 44 唯一 (lead,type) 对（内容完全一致才删，否则 abort）。**消除双发隐患**。
  - 回收 `2026-08-19`（9 条从未发送）+ `canary_2026-08-20`（1 条测试产物）→ cancelled。
  - 撤销 `auth=27`(p1_2)、`auth=30`(canary) → revoked（`send_authorizations` 无 skip_reason 列，仅改 status）。
  - 备份：`data/bd_leads.db.bak_20260820_112826`（sha256 88efc8ce…d846ae）。
- **最终核验**：`2026-08-20` planned=44 / 重复对=0 / `2026-08-19`=0 / canary=0 / auth 27&30=revoked；生产函数 `check_stale_objects('2026-08-20')` = **pass**（"no old plans/auths; poller fresh"）。stale_objects 闸门对当前活跃批次已通过。
- **附带观察（非 stale 范围）**：`2026-08-20` 含 4 个 `follow_up` lead（7/37/40/48，过去批次已发 new_outreach）；去重后各留 1 条 follow_up。若当前政策 follow-up 禁用，这些行本就不该被 outreach 消费——属政策判断，未改。

## P2.1G — FIRST REAL AUTOMATED SEND OBSERVATION（用户指令）
- **目标**：观察修复后真实自动化发送链（不修改代码/不新建任务/不手动建 FSP，等现有 Task Scheduler 22:30 pre-send → 23:00 outreach → 00:10 post-send）。
- **关键发现（重要）**：`job_runs` 显示今天 **02:00 已跑过一轮旧 schedule 残留**（pre-send 两次 01:49/02:00 用修复前旧代码 → 累积 88 行，即被清理去重的来源；outreach 两次均 `stopped/outside_send_window` 0 发出；post-send 一次 completed）。`send_log` 2026-08-20 当前 = 0 行。**今晚 22:30/23:00/00:10 才是首次在「窗口开启」时跑修复后代码**——23:00 outreach 真正进入 23:00–23:59:30 窗口，是 P2.1G 要验证的真实首次自动发送。
- **调度器武装确认（read-only schtasks /query）**：3 个 Task 均 ENABLED/就绪，下次运行 22:30/23:00/00:10。**风险点**：登录状态均为 `只使用交互方式`（interactive only）——机器须登录且未睡眠才会触发，这正是 P2.1G 要验证的。
- **幂等验证预期**：今晚 22:30 pre-send（新代码）会先退役既有 44 planned 再插入 44 → 仍 44、0 重复对，证明不翻倍（旧代码会到 88）。
- **交付（只读验证工具，不改生产代码）**：新建 `roktandrazo-outreach/_p21g_observe.py`——baseline 对比（output/p21g_baseline.json）、按今晚 run 窗口过滤、区分 new_outreach/follow_up、输出 AUTOMATED_RUN/PRESEND_OK/FSP_CREATED/DUPLICATE_FSP/OUTREACH_OK/SMTP_ACCEPTED/BOUNCE/REPLY/POSTSEND_OK/FULL_LOOP_PASS，内置 `--watch` 模式轮询。
- **当前状态（11:59）**：AUTOMATED_RUN=pending；基线 2026-08-20 planned=44/dup=0/follow_up=4/new_outreach=40/stale_objects=pass；manual_pause=false/standing_authorization=true/risk_gate_status=clear。已启动 `--watch` 后台守程（task_id Vq9LEF），约 00:15 跑完或 02:30 硬超时自动出报告并通知。

## P2.1H — PRODUCTION ENTRYPOINT AUDIT（只读，用户指令：只报告不清理）
- **A. 发送入口**：ACTIVE_SEND_ENTRYPOINTS = 唯一 `RoktRazo-BD-Outreach`(23:00) → `bd_orchestrator --stage outreach --live` → `daily_session.execute_final_send_plan` → `bd_sender.send_one`(SMTP)。LEGACY = `RoktRazo-BD-Daily-Outreach`(DISABLED, daily_operator_auto --live)、`Roktandrazo_Daily_Outreach`(DISABLED, daily_runner --dry-run)、代码层 main.py/sender.py/pipeline.py/daily_operator_auto.py（含 SMTP 能力但均未接任何活跃触发器，sender.py 已 fail-closed）。
- **B. Windows Task**：9 个 RoktRazo-BD 相关任务。仅 3 个生产链 + Outreach(23:00 唯一发信) 是 Ready 且用 bd_orchestrator；另有 Morning(status dry-run)/Inbox-Morning & PostSend-Poll(inbox_polling_monitor, 无 SMTP)/Inventory-Recovery(inventory_recovery_loop --no-dry-run, 无 SMTP) 均 Ready 但非发信；2 个 Daily-Outreach 均 **Disabled**。CAN_TRIGGER_SMTP=true（仅 Outreach 一处）。唯一入口链 bd_orchestrator→daily_session→send_one = **成立**。
- **C. Automation**：ACTIVE 的 WorkBuddy 自动化里 `BD Production Outreach`(1785804421539, 22:00) 已 **PAUSED**；其余 ACTIVE 的 Pre-Send(1785804406748,21:30)/Preflight(1785804413719,21:50)/RecoverySync(1786002601925,08:45) 均 **NO SMTP**。即无活跃自动化直接发信。
- **D. DB**：planned 仅 `2026-08-20`(new_outreach=40/follow_up=4, 0 重复对)；旧批次 2026-08-19 已 cancelled=10；send_authorizations 状态 consumed=5/revoked=6/superseded=6 → **0 个 approved/active**。生产函数 `check_stale_objects('2026-08-20')` = **pass**。
- **结论**：PRODUCTION_SEND_PATH_CLEAN=**false**（发信链唯一安全，但存在**双 pre-send 触发器冗余**：WorkBuddy 自动化 Pre-Send(21:30) 与 Windows Task PreSend(22:30) 都为同一批次 2026-08-20 建 FSP；且 agent 自动化 prompt 指定的 batch_id 约定 `new_outreach_YYYYMMDD_et1000` 与 bd_orchestrator 实际的 `2026-08-20:new_outreach:<uuid>` 不一致，若 agent 手工建 FSP 而非调用 bd_orchestrator，会令 `check_stale_objects` 标旧 planned 行 → STALE FAIL）。无第二发信路径（不会双发）。CONFLICTING_SEND_PATHS=上述双 pre-send；MODIFICATION_REQUIRED=false（按指令只报告）。建议后续：要么停用 WorkBuddy Pre-Send/Preflight 自动化（Windows Task 已接管全链），要么确保 agent 自动化只调 bd_orchestrator 不手工建 FSP。
