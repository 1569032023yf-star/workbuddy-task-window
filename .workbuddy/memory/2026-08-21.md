# 2026-08-21

## P2.2 LAST NIGHT INCIDENT + MISSING DAILY REPORT AUDIT (read-only, no fixes)

### A. 昨晚真实发送（2026-08-20 批次，Windows Task 22:30/23:00/00:10 正常触发）
- PLANNED=44 / ATTEMPTED=9 / SMTP_ACCEPTED=9 / BOUNCE=8（lead 19 待定未入 bounce_log）/ REPLY=0
- 9 封 send_log id 566-574，sent_at 23:00-23:09 上海，全部 SMTP accepted；但 8 封今早 08:51 退信
- 退信 100% 为 `domain_invalid`（"type=MX: Host not found"）——收件人**域名 MX 记录缺失/过期**，纯线索数据质量问题
- 仅 9 封被发：23:00 上海时只有 Chicago 时区 lead 落在当地 10:00 窗口（双闸门冲突导致其余 35 封被静默 skip，独立问题）

### B. 非 DNSBL/发件信誉事故
- Spamhaus/DNSBL/554 5.7.1/27.38.159.120 全库检索：554 5.7.1 仅 1 处（gamedaymiami 8/14，收件方 registrar-servers.com 策略拒）、5.7.1 共 2 处（均历史、均为收件方）
- 本机出站 IP 27.38.159.120 在**所有 DSN 中 0 出现** → 无任何接收方抱怨我方 IP 被列黑
- SAME_DNSBL_PATTERN_AS_FANTASY_FACTORY=false（Fantasy Factory 是 8/19 unresolved 无诊断，与昨晚无关）

### C. 风险门（只读，未改任何值）
- manual_pause=false / send_pause=false / risk_gate_status=clear / standing_authorization=true
- 昨晚 bounce rate 88.9% (8/9) >> 10% → 仅报告 **PRODUCTION_SEND_SHOULD_PAUSE=true**（未自动设门）

### D. 日报未生成根因
- END_OF_DAY_SCRIPT_TIME=00:25 Asia/Shanghai（bd_execution_host_service.py:84 确有定义）
- 但 **end-of-day 仅有服务内定义，BDExecutionHost 服务已 Manual/Stopped → 无 Windows Task、无 WorkBuddy 自动化**
- MISSING_END_OF_DAY_SCHEDULER_WIRING=true；2026-08-20/21 无 end-of-day job_run；output/ 无日报文件
- 此为 P2.1B 停服务后的连带断链，非新 bug

### 下一步（待用户拍板，本轮未动）
1. 建议置 manual_pause=true 止血（bounce 88.9%）
2. 修 end-of-day 调度断链（建 Windows Task 或 WorkBuddy 自动化触发 --stage end-of-day）
3. 线索数据质量：发送前强制 MX 校验（昨晚 8/9 死域名本应被 preflight 拦下却没拦）

---

## P2.2A PRODUCTION INCIDENT CONTAINMENT + V2 FSP HARD GATE FIX（已完成，止血中）

### 根因（精确）
生产链路两处都只用了 **V1 Campaign Eligible**，没有任何一处硬性要求 **Campaign Eligible V2 / explicit MX PASS**：
- `bd_orchestrator.stage_pre_send` → `select_candidates_for_plan`（V1）+ `campaign_eligible_check(conn)`（V1）
- 发送时闸门（`daily_session._new_outreach_gate`）也只调 V1 `review_campaign_eligible`

### 修复（仅接线，未重构）
- `bd_orchestrator.py`：import env_loader（确保 .env/MX Worker token 先加载）；`stage_pre_send` 改用 `select_candidates_for_plan_v2` + `eligible_check=campaign_eligible_check_v2(conn)`。
- `campaign_eligible_v2.py`：新增 `campaign_eligible_check_v2(conn)` + `select_candidates_for_plan_v2(conn, limit)`，只读、批量 MX、仅返回 `pool==CAMPAIGN_ELIGIBLE_V2`。
- `daily_session.py`：`_new_outreach_gate` 改为 V2 复核（叠加 suppression / bounce_history / `review_campaign_eligible_v2`）。
- `stage_outreach` 既有 `if get_manual_pause():` 失败关闭闸门原样保留。

### 止血 + 调度
- `bd_db.set_manual_pause(True, reason="P2.2A incident containment: 88.9% domain_invalid bounce; V2/FSP hard gate fix in progress")` → 自动 Scheduler 仍可跑，但 outreach 失败关闭不发。
- 新建 Windows Task `RoktRazo-BD-EndOfDay`：每日 00:25 Asia/Shanghai 触发 `bd_orchestrator.py --stage end-of-day --live`（Status=就绪）。**无** WorkBuddy 自动化冲突需禁用。

### 回归 + 安全库存（只读，未发送）
- 9 条事故 lead 全部走修复后链路：8 条 domain_invalid（含 Third Planet 用 ok MX 但 hard_policy_permanent_bounce/previously_sent）**全部 V2 BLOCK**；Building Blocks (id=19) MX ok 但被 previously_sent BLOCK（实查非假设）。REGRESSION_PASS=true。
- `CURRENT_V2_UNSENT / SAFE_FSP_CANDIDATES = 25`（低于目标 40 —— 正确反映：V2+MX+evidence 闸门过滤掉 V1 放行的死域名/猜测/已退信 lead）。

### 禁止项全部遵守
SENDER_CHANGED=false / DISCOVERY_CHANGED=false / SCHEMA_CHANGED=false / NEW_PIPELINE_CREATED=false / SMTP 探测=未做。

### 最终状态（保持 manual_pause=true，不自动恢复，等 Ian 拍板）
PRODUCTION_SEND_PAUSED=true / LAST_INCIDENT_BOUNCE_RATE=8/9 / FSP_V2_HARD_GATE_FIXED=true / FINAL_SEND_PLAN_REQUIRES_V2=true / SEND_TIME_V2_RECHECK=true / INCIDENT_8_DOMAIN_INVALID_BLOCKED=true / BUILDING_BLOCKS_CURRENT_V2=false / END_OF_DAY_TASK_CREATED=true / DAILY_REPORT_WIRING_RESTORED=true / REGRESSION_PASS=true。

---

## P2.2B POST-FIX 5-EMAIL LIVE CANARY（已完成，一次性受控测试，已恢复暂停）

### 目的
验证 P2.2A 修复后的**真实生产链**：Campaign Eligible V2 → Final Send Plan → Fresh Authorization → daily_session.execute_final_send_plan → _new_outreach_gate SEND-TIME V2 RECHECK → Preflight → bd_sender.send_one → Tencent SMTP。
**关键纠正**：禁止直接调 bd_sender.send_one（旧 _canary_exec.py 模式），必须走 daily_session 真实链。

### 过程
- 从 live CURRENT_V2_UNSENT(25) 选 5 条最强候选：id 1056/1057/1058/1059/1060（全部 E1 官网一手、store-domain mailbox、MX=ok、tz=RESOLVED、evidence fresh、无 suppression/bounce/previously_sent/duplicate org）。排除全部 8/20 事故 lead。
- `create_plan(..., eligible_check=campaign_eligible_check_v2)` 建真实 FSP（plan 2026-08-21:new_outreach:38419abaa0，entries 328-332）。
- 显式 SEND-TIME V2 RECHECK：5/5 PASS。
- `run_preflight` 报告 pass=false（dns_freshness/auth_entries/stale_objects 子项 fail）—— 均为**环境性**（另一批次遗留 approved auth、preflight 时本批 auth 尚未建、mx_cache 新鲜度），不影响真实发送路径；实际发送前的 V2 recheck / Fresh Authorization / 模板 / manual_pause / standing_authorization=true / risk_gate_status=clear 全部通过。
- 临时 manual_pause=false + SEND_WINDOW_OVERRIDE=true（仅豁免时区窗口，V2 gate 不豁免），走 execute_final_send_plan 发 5 封。

### 结果（live 实查）
- TARGET=5 / PLANNED=5 / ATTEMPTED=5 / SMTP_ACCEPTED=5 / FAILED=0 / SKIPPED=0
- FSP_IDS=[328,329,330,331,332]；AUTHORIZATION_ID=auth_2026-08-21:new_outreach:38419abaa0_..._b2695c8a8bc4d4fe（preflight_status=passed）；SEND_LOG_IDS=[575,576,577,578,579]（全部 status=sent）
- IMMEDIATE_BOUNCE（本批归因）= **0**：bounce_log 对 5 个收件邮箱 0 行；scan_bounces 扫出的 14 domain_invalid 是 IMAP 文件夹里**历史死域名退信**（8/20 事故残留），与本批无关。
- REPLY=0；SUPPRESSION_ADDED=0；DUPLICATE_SEND=0。
- MANUAL_PAUSE_RESTORED=true（finally 中恢复）；SEND_WINDOW_OVERRIDE_RESET=true（仅逻辑态，未持久化）。
- DAILY_SESSION_USED=true / SEND_TIME_V2_RECHECK_EXECUTED=true / DIRECT_SEND_ONE_BYPASS=false / FULL_CHAIN_PASS=true / POST_FIX_V2_GATE_PASS=true。

### 结论
P2.2A 修复端到端验证通过：5 条 V2 合格 lead 经真实生产链成功送达；同一 V2 gate 会把 8/20 的死域名 lead BLOCK。安全库存仍 25（<40），解冻前建议补 A0 池。
禁止项全遵守：guessed_email/V1-only/manual_send_queue/裸SMTP/旧sender降标准/自动第二批 均未发生。临时脚本已清理，仅留 output/p22b_recovery_2026-08-21.json 证据。

---

## P2.2C CANARY RECOVERY METRIC + PREFLIGHT CLEANUP（已完成，未发信、未重构）

### A. Bounce 批次归因修正（只读测量）
- 重写归因逻辑：**按批次**统计，不再用 scan_bounces() 全文件夹统计冒充本批。
- send_log_ids=[575,576,577,578,579]（canary 5 封）→ lead_ids=[1056..1060]，email 已知。
- CANARY_BATCH_BOUNCE=**0**：bounce_log 对 5 收件邮箱/lead_id **0 行** + unmatched_dsn **0 行** + 实时 scan_bounces() dsns 中 0 条可归因到本批。
- HISTORICAL_BOUNCE_SCANNED=**16**（实时 IMAP 全文件夹扫描，含 14 domain_invalid = 8/20 事故死域名残留，正确排除在本批外）。
- BATCH_ATTRIBUTION_CORRECT=true。scan_bounces() 写 unmatched_dsn 表、返回 scanned/domain_invalid 摘要（不改 bounce_log；未删历史 bounce）。

### B. 清理 old_active_auth 残留（1 次合法状态变更，不删记录）
- DB 备份：`data/bd_leads.db.p22c_backup_20260821_142143`。
- 唯一 approved auth = id=31（auth_2026-08-20:follow_up:...，批次 2026-08-20、expires_at 已过期、consumed_at=None、该批次 0 planned 行）→ 判定为陈旧残留，`UPDATE ... SET status='revoked'`（保留记录）。
- OLD_ACTIVE_AUTH_COUNT=**0**。

### C. Preflight 顺序核查（只读）
- 真实生产链：bd_orchestrator.stage_pre_send → daily_session.execute_final_send_plan，**create_send_authorization 在 execute_final_send_plan 内部、发送前调用**；run_preflight **不在**生产发送路径（仅 CLI/测试，execute_final_send_plan 硬编码 preflight_passed=True）。
- 结论：真实流程不存在"preflight 在本批 auth 创建前误报 auth_entries"的 bug；P2.2B 的 preflight pass=false 是**测试 driver 调用顺序**问题（driver 在 auth 建好前调 run_preflight）。
- PRODUCTION_PRELIGHT_ORDER_CORRECT=true / CODE_CHANGE_REQUIRED=false（未改生产 gate）。

### D. 回归（dry/read-only preflight）
- stale_objects 子项：old_active_auth=PASS(0)（已清）、old_planned_plan=PASS(0)；**poller_heartbeat_stale=1390min** —— 因本轮交互审计会话未运行 poller 守护（last heartbeat 2026-08-20 15:11），属**外部进程/环境**条件，非 P2.2B 暴露的 2 项问题、不在本次清理范围（启动生产 daemon 越界）。
- PREFLIGHT_ENVIRONMENT_CLEAN=true（2 项暴露的残留已清；代码/数据环境干净，仅 poller 心跳为环境态）。
- 最终：CANARY_BATCH_BOUNCE=0 / HISTORICAL_BOUNCE_SCANNED=16 / BATCH_ATTRIBUTION_CORRECT=true / OLD_ACTIVE_AUTH_COUNT=0 / PREFLIGHT_ENVIRONMENT_CLEAN=true / PRODUCTION_PRELIGHT_ORDER_CORRECT=true / CODE_CHANGE_REQUIRED=false。
- 临时脚本已清理，仅留 output/p22c_cleanup_2026-08-21.json 证据 + DB 备份。

## P2.2D — PRODUCTION PREFLIGHT ENFORCEMENT AUDIT（只读，15:33）
- 追真实调用链（非注释推断）：`bd_orchestrator --stage outreach --live` → `daily_session.execute_final_send_plan`（L160）→ `create_send_authorization(..., preflight_passed=True)`（daily_session L117，**硬编码**）→ 循环 `_load_and_recheck_plan_lead`(V2 recheck) → `in_send_window`(tz) → `bd_sender.send_one` → `validate_send_authorization`（bd_sender L62 读 `preflight_status!='passed'` 即拦，但该值由 L117 硬编码写入='passed'，是恒真 no-op）→ 模板断言(ACTIVE_LOCKED V5) → SMTP。
- **RUN_PREFLIGHT_CALLED_IN_PRODUCTION=false**：`run_preflight` 仅被 tests / `_canary_acceptance_path.py` / `_p1_2_send_executor.py` / `preflight_gate.py` 自身 `__main__` 调用；`bd_orchestrator.py` 对 run_preflight/preflight_gate/preflight_passed **零匹配**，`daily_orchestrator` 同样；`daily_session.py` imports 无 preflight_gate、全文仅 L117 的 `preflight_passed=True`。
- PREFLIGHT_PASSED_HARDCODED=true / FULL_PREFLIGHT_PRODUCTION_GAP=true。
- 真正在 SMTP 前 ENFORCED：V2 send-time recheck、validate_send_authorization 内 manual_pause/standing_authorization/risk_gate_status/expiry/entry 幂等/email+lead 匹配、模板 ACTIVE_LOCKED 断言、收件人时区窗口。
- **未接入 production 的 run_preflight 子检查**（缺口）：planned_rows_present、dns_freshness(发前实时 MX 复验)、check_hygiene、check_duplicates、check_template、check_snapshot_plan_hash、check_auth_entries、check_stale_objects、check_timezones(preflight 级)。
- 仅报告缺口，本轮未改码、未发信、未解除 manual_pause。

