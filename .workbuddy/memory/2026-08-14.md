# 2026-08-14

## P1.2 First 20 Supervised Send — 昨晚窗口执行复盘 (09:04-09:10 CST)
### 昨晚 (8-13) 结果
- ET/CT 窗口均被 Preflight BLOCKED → **0 封发送（正确 fail-closed）**
- 阻断原因:
  1. auth_entries: auth status=superseded (我昨晚清理重复 auth 时误把有效 auth superseded)
  2. stale_objects: poller_heartbeat_stale 283min (窗口到达时 poller 未刷新)
- 20 条 fsp 仍 planned，SMTP=0

### 修复 (仅 Scheduler/Window Gate，Canonical Chain 冻结)
- 重建 auth (20 entries approved, 匹配真实 fsp.id)
- _p1_2_send_executor.py 增强:
  - 窗口前 refresh_poller() (真实跑 health/reply/bounce/tracking)
  - 窗口前 rebuild_auth() (auth 15min 过期, 窗口到达必须 fresh)
  - 修复 rebuild_auth 的 database locked (DELETE 后 commit 再 create)
- 验证: rebuild auth + poller + preflight = PASS=True

### Timezone Plan (今日窗口)
- ET 10条: 上海 21:55-22:10
- CT 10条: 上海 22:55-23:10
- RECIPIENT_LOCAL_10_SCHEDULER_READY=true

### 执行器
- 今日执行器已启动 (PID 6864, 09:07), 等待今晚窗口自动发送
- 每个 batch 窗口前: 刷新 poller → 重建 auth → preflight (fail-closed) → send_one → reconciliation

## P1.2 Immediate Supervised Send — 一次性 SEND_WINDOW_OVERRIDE 执行完成 (09:19-09:56)
### 用户授权
- 不再等待 Recipient Local 10:00，本次一次性 override 时间窗口
- 永久 Scheduler 规则 (Recipient Local 10:00) 保持不变；标记 TIMING_EXPERIMENT=OFF_SCHEDULE_OVERRIDE
- 只绕过 recipient_scheduler.in_send_window()，其余 Gate 全部 fail-closed

### 代码修改（最小参数化，永久规则不变）
- daily_session.execute_final_send_plan(batch_date, dry_run, send_window_override=False)：仅跳过 in_send_window 检查
- preflight_gate.run_preflight / check_timezones：加 send_window_override（只豁免 expired 窗口判定，TIMEZONE_UNRESOLVED 等仍 fail-closed）
- _p1_2_send_executor.py：新增 send-now 模式（每轮刷新 poller+重建 fresh auth+preflight+execute_final_send_plan），auth 15min 有效期需分轮
- bd_sender.send_one 模板断言修复：template_key 回退 template_id（leads 表无 template_key 列）

### 执行结果（20/20 SMTP Accepted）
- 20 条 FSP (153-172) 全部 sent，send_log 20 条，Message-ID 20/20
- 首轮 13 条成功后 auth 过期，7 条被 fail-closed 拦截 → 修复执行器 (SendAuthorizationError 恢复 planned) 后第 2 轮补发完成
- Post-Send Reconciliation: PASSED (total_issues=0)，consumed auth entries=20

### 过程中修复的 bug
1. send_one 模板断言读 lead.template_key（leads 无此列）→ 回退 template_id → 首次发送 20 条全 BLOCK
2. rebuild_auth 每轮 DELETE 全部旧 auth（含已 consumed）→ reconciliation 13 条 no_auth_entry → 改为只删未消费 auth，恢复 13 条审计记录
3. 执行器 remaining 只统计 planned → 改为 planned + 可重试 SendAuthorizationError

### 遗留/注意
- tracking_token_present_count=0：.env 缺 EMAIL_OPEN_TRACKING_ENABLED=true（默认 false），渲染阶段无 pixel/token。非本次引入
- leads sent total=393 (含今日 20)；leads total=1017
- 本批次结果不计入"当地 10:00 有效性"判断

## EMAIL_RUNTIME_BASELINE 只读核实 (11:13-11:50)
### 邮箱基础设施（真实）
- Outbound: smtp.exmail.qq.com:465 SSL, user=ianyf@roktandrazo.com, From=Ian <ianyf@roktandrazo.com>（腾讯企业邮箱 Exmail）
- Inbound: imap.exmail.qq.com:993 SSL, 同一账号 ianyf@roktandrazo.com
- send_log 写入 data/bd_leads.db (bd_db.log_send)，DB 路径 WORKBUDDY_BD_DB_PATH 可覆盖

### 关键真相（易被误判）
1. poll_bounce（bd_ops_poller）**不扫 IMAP**，只把 bounce_log 的 hard bounce 同步到 suppression_list
2. 真实退信 IMAP 扫描只有 bounce_pipeline.scan_bounces（run_scan_and_writeback），唯一入口 result_recovery_sync 08:45——但**无任何计划任务/服务调度它**，仅由外部人工触发
3. inbox_polling_monitor.py（计划任务 Inbox-Morning/PostSend-Poll）是**只读分类器**，checkpoint 停在 7-14，任务 result=1 失败，不工作
4. BDExecutionHost Windows 服务**不存在**，poller 无常驻进程；P1.2 期间 poller 是执行器 refresh_poller 临时启动的
5. poll_reply 用 From 地址匹配 send_log.email → 会把 sender copy（From: Ian <ianyf@roktandrazo.com>）误判为 customer_reply，8-14 已产生 81 条 lead_id=0 污染记录
6. P1.2 有 10 封真实退信（deepcomics/legitmtg/31st-century/unrealcitycomics/treasurecoasttoys/gamedaymiami/dicecafestl/bpluscomics/fantasyshoponline/d4seasons）在 INBOX，但 bounce_log/unmatched_dsn/suppression 8-14 零新增——漏层=发送后无 bounce scan 运行

### Source of Truth 顺序（重要）
- Delivery: 企业邮箱真实 DSN → bounce_pipeline parse → bounce_log/unmatched_dsn → dashboard
- Send: SMTP transaction → send_log → FSP → dashboard
- Reply: 企业邮箱原始 reply → scanner(需修 self-sent 过滤) → reply_log → dashboard
- 不要把 dashboard / poller status.json 当第一真相源（两套写入路径可能互相覆盖）

## P1.3 Delivery Quality Recovery (11:47-12:00)
### 触发：P1.2 Bounce Rate 高（SMTP 20 / 真实退信 15 = 75%）
- 注意：P1.2 真实退信 **15 条**（非初估 10；IMAP 全量扫描确认）
- 分类：domain_invalid 12 (80%) / mailbox_invalid 2 (13%) / policy_bounce 1 (7%)
  - 12 条 domain_invalid 诊断=type=MX: Host not found（域名无有效 MX）
  - 2 条 mailbox_invalid 真实 SMTP：gigabitescafe 550 5.1.0 Recipient not found、deepcomics 550 No Such User
  - 1 条 policy：gamedaymiami 554 5.7.1
- **根因：地址质量**（非信誉）。绝大多数是 guessed_email + 无 MX 验证

### 数据恢复（备份 backup/bd_leads_p1_3_20260814_114820.db）
- bounce_log 8-14: 15 条（全部 matched send_log）
- unmatched_dsn 8-14: 15 条（全 matched_send_log，0 条真 unmatched）
- suppression 新增 15 条（auto_bounce_*），总 41
- leads 回写：12 domain_invalid + 2 mailbox_invalid → status='bounced', email_sendable=0, contact_recovery_required=1；1 policy → delivery_policy_review=1

### Fake Reply 修复（大发现）
- reply_log 441 条中 **440 条是 fake**（email=ianyf@roktandrazo.com self-sent 误判），仅 1 条真实（lead 237 OOO 6-25）
- 根因：poll_reply 用 From 地址匹配 send_log.email → sender copy（send_one 自发 untracked 副本进 INBOX）被误判
- 修复 poll_reply：From==sender→ignore；matched lead_id==0→never customer_reply；sender copy Message-ID→ignore
- 已删 440 条 fake，保留 1 条真实；修复后重跑 0 新增

### poll_bounce 修复
- 原 poll_bounce 不扫 IMAP 只读 bounce_log→suppression → 已改：
  - poll_bounce 现在调 bounce_pipeline.run_scan_and_writeback()（真实 IMAP 扫描）
  - 新增 poll_suppression_sync（原 suppression 逻辑，覆盖 domain_invalid/mailbox_invalid）
  - start_poller 加入 suppression_sync 线程

### Post-Send Result Recovery（新，_p1_2_send_executor.py）
- run_post_send_recovery()：bounce scan + writeback + unmatched + suppression + reply + tracking
- send-now 与 send 模式完成后自动调用（不依赖 08:45 sync）
- 已实测 ALL_OK=True，落盘 output/post_send_recovery_*.json

### 结论
- NEXT_BATCH_SEND_PAUSED=true 保持；下一批需加强 Email Quality Gate 后选 10 条验证

## P1.4 Email Quality Gate V2 (14:42-15:10)
### 实现（campaign_eligible_v2.py，新文件）
- V1 规则全保留（复用 review_campaign_eligible）；新增 4 层：
  - MX Verification 硬门禁（preflight_gate.query_mx Worker 查询）：ok 通过 / nxdomain/null_mx/no_mail_route BLOCK / dns_error RETRY_PENDING（不 PASS）
  - Email Source Tier：E1 official_page_visible+verified / E2 web_search_official / E3 公共邮箱+官方证据 / E4 guessed_email（高风险需加强验证）/ BLOCK
  - guessed_email 加强验证：MX PASS + 官网域匹配 + 官网可访问 + evidence fresh，否则 NEEDS_EMAIL_VERIFICATION
  - Evidence Freshness：evidence_checked_at（回退 last_checked_at）>90 天 → EVIDENCE_STALE

### V2 漏斗结果（真实，用 Worker MX 预查）
- 原 54 条旧 Eligible：MX PASS=20 / MX BLOCK=34（33 nxdomain + 1 no_mail_route）/ RETRY=0；Tier E4=47, E1=3, E2=4；**CAMPAIGN_ELIGIBLE_V2=0**
- 全库 119 未发送候选：MX PASS=62 / MX BLOCK=57；Tier E1=15, E4=99, BLOCK=4, E2=1；**CAMPAIGN_ELIGIBLE_V2=0**
- 未发送池 34 条 V1 eligible：全 E4 guessed_email，MX PASS 仅 9，全 NEEDS_EMAIL_VERIFICATION
- 关键发现：E1 的 15 条全被 broad_ready 拦截（previously_sent/suppression/third_party）→ 库存中无"未发送+MX PASS+官方证据"的可发候选
- 数据说明：34 条旧 eligible 大量 guessed_email（12/15 P1.2 退信即此类）；evidence_method 全空、evidence_checked_at 仅 2 条有值（已回退 last_checked_at）

### 产物
- output/v2_funnel_54.json（54 条明细）、output/v2_inventory_all.json（119 条明细）、output/v1_eligible_ids.json
- 未改任何 lead 状态（只读验证）；P1.2 的 15 条 bounce 未解锁

### 结论
- EMAIL_QUALITY_GATE_V2_READY=true（实现+验证完成）
- CAMPAIGN_ELIGIBLE_V2_COUNT=0 → 无 Top 10 可发；下一批需先补库存（新 Discovery 或对 guessed 候选加强验证）
- NEXT_BATCH_SEND_PAUSED=true

## P1.5 Verified Lead Recovery (15:20-15:50)
### Lane A — Existing Verification（10 条 guessed_email 候选官网验证）
- **全部 10 条验证失败，无任何候选通过第一方官网邮箱验证**
- 分布：NO_EMAIL 3（Shuffle&Cut 仅表单/电话、Maximum Comics 仅电话 702-367-0755、MoMA 客服走表单）/ UNREACHABLE 3（Game Haven、Games&Gizmos、Diversions，http+https 均无法连接）/ DOMAIN_FOR_SALE 2（Third Planet GoDaddy 出售、Eternal Games Sedo 出售）/ SITE_NOT_FOUND 1（eparks DreamHost）/ DOMAIN_HIJACKED 1（Top Ten Toys → Europe777 赌场站）
- 证实 guessed_email 候选官网多为失效/无公开邮箱 → 维持 NEEDS_EMAIL_VERIFICATION，不发送

### Lane B — Quality-First Discovery
- web_directory 可抓取（TN 15/AR 9/KY 14）但 **website 字段为空，目录本身无邮箱** → 无法自动产出 verified email
- 无 API key（SERPAPI/GOOGLE_MAPS 均未配置）→ 无法走 Places/SerpAPI 带邮箱发现
- 新抓 store 大多已在库（Dewayne/Dicehead/Bald Man/Galaxy/Campus Cards），且无邮箱

### 库存复核
- 未发送 + verified=1 + 未 suppression 候选仅 8 条：全部被 broad_ready 拦截（Idaho Taters 已发 7-29、Dicehead 已发、其余 third_party 域或 ACCEPTANCE_TEST）
- 结论：**当前库存 Verified Eligible V2 = 0，无可用 verified 发送候选**

### 判定
- CAMPAIGN_ELIGIBLE_V2_COUNT = 0
- READY_FOR_V2_10_SUPERVISED_SEND = false
- 需：配置 API key（SerpAPI/Google Places）+ 官网邮箱抓取，或人工浏览器验证新候选

## P1.6 Verified Lead Production Test (15:41-16:10)
### 输入：30 家从未发送的新商户（web_directory 全州抓取 376 家去重后选 30 家独立店）
### 官网验证（3 个 Agent 并行，严格第一方规则）
- 漏斗：New Stores 30 → Website Found/Valid 12 → Official Email Found 6 → MX PASS 6 → Eligible V2 2
- 损耗：NO_WEBSITE 14(47%) / WEBSITE_INVALID 3(10%) / NO_EMAIL 7(23%) / EMAIL_FOUND 6(20%)
- EMAIL_FOUND 6 家：Bald Man / Gear Gaming / Armada / Cool Stuff / Emerald City / Fallout
  - 3 家已在库且已发送（Gear id418、Armada id235、CoolStuff id232）→ 排除
  - Emerald City 同组织已发（lead 237 chad@ 已 send_log 70）→ cross_check 拦截
  - **全新入库 2 家：Bald Man Games (1053, vddw@baldmangames.com)、Fallout Comics (1054, info@falloutcomics.com)**
### V2 判定
- 两家均 MX PASS + E1 official_page_visible + evidence fresh + 补时区/组织键后 **CAMPAIGN_ELIGIBLE_V2** ✅
- **CAMPAIGN_ELIGIBLE_V2_COUNT = 2 < 10** → 按规则停止扩量
- VERIFIED_EMAIL_YIELD = 2/30 = 6.7%
### 结论
- 主要损耗层：找不到官网(47%) + 官网无公开邮箱(23%) + 官网失效(10%)；EMAIL_FOUND 中一半是已在库/已发
- 达到 10 需更大批量或配置 Discovery API（SerpAPI/Google Places 带官网/邮箱）
- NEXT_BATCH_SEND_PAUSED=true





