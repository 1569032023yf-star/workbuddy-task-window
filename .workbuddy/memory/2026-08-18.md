# 2026-08-18 — P1.8 Emergency BD Batch + Management Report

## Canary 生产链路验证（已执行）
- 09:21 上海时间发出 1 条 Production Canary（SEND_WINDOW_OVERRIDE=true，TIMING_EXPERIMENT=OFF_SCHEDULE_CANARY）。
- CANARY_LEAD = Game Cafe（id=1055，tom@playgamecafe.com，store-domain 官方邮箱，game store → retail_distributor_v5_locked）。
- 链：bd_template.apply_email_to_lead → final_send_plan.create_plan (FSP id=173) → bd_sender.create_send_authorization (preflight_passed=True) → bd_sender.send_one。
- SMTP Accepted=YES（send_log id=555, status=sent）, Message-ID=`178701607874.6280.1405454414691365213@roktandrazo.com`。
- 即时 DSN 扫描 Bounce=0（命中的 16 条 DSN 为历史旧批，非本 Canary）。Reconciliation 一致。
- 关键：MX Worker 必须用 .env 的 TRACKING_DASHBOARD_API_KEY（硬编码 token 已过期会返 401→dns_error）。scan_v2_inventory 前需 os.environ 注入 .env。

## 今晚主批次
- V2 底仓 11 条已锁定；1055 已作 Canary 发出，ET 余 6 + CT 4 = 10 待今晚（ET~22:00 / CT~23:00 上海）。
- 安全阈值：任一批前 10 条 bounce≥2(≥20%) 暂停剩余批次。

## 报告
- BD_STAGE_REVIEW_20260818.md 已建（一页领导版 + 附录 + 60s 口头版 + Canary 结果）。主批次发送后回填汇总字段。
- TASK A 继续补 V2（目标池→30，不阻塞今晚 11 条）；TASK B 报告草稿已完成。

## 注意
- 真实发送必须走 bd_sender.send_one（P0 Authorization gate，fail-closed）。test_mode 必须 OFF 才是真生产 canary。
- .env 含 SMTP（iwanyf@roktandrazo.com 腾讯企业邮箱 465）与 MX Worker token。

## 晚班次补池（Thread 2，只收集不改系统）—— V2 库存 32 ≥ 30 ✅
- 权威扫描 `_final_v2_scan_dns_0818.py`（MX Worker 宕机期间给 preflight_gate.query_mx 打 dnspython 补丁，不改系统逻辑）→ **CAMPAIGN_ELIGIBLE_V2=32**（ET14/CT10/MT1/PT7）。
- 批次 0818c 插入 5 家：Blue Bridge(MI)/Total Escape(CO)/Portland Game Store(OR)/Time Warp NJ/Red Castle(OR) → ids 1069–1073。
- 批次 0818e 插入 5 家 WA：Comics Keep/Phantom Zone/Subspace/Arcane/Outsider → ids 1074–1078，evidence_method=web_search_official。
- **关键教训：DB 已深度覆盖知名店** — Guardian/Card Kingdom/Blue Highway/Game Goblins/Sci-Fi City/Grand Adventures/Comic Book World 全部已存在（sent/bounced/delivery_issue），cross_check 智能拦截（SQL 按 domain_hash/email/store_name 预查会漏，insert_lead 内部 cross_check 才是真闸）。
- **数据问题（人工复核）**：id=802 Grand Adventures(gmail,new) 与 id=701(sent) 同一店但 org_key 不同 → 802 漏过 shared_domain_org_history 进了可发送池。日报已标记。
- 候选验证模式：WebSearch 批量预筛 → SQL dedup + resolve_timezone + 域名匹配 + dnspython MX → insert_lead(+enrich org_key/tz/mx/auto_sendable) → review_campaign_eligible_v2 真实门控验证。
- 今晚 10 条 queued（FSP 174–183，ET7/CT3）仍被暂停的 automation-1785804421539 阻塞，待用户决策。

## 15 天复盘报告（只读统计，不改系统）
- 产出 `BD_15DAY_SEND_PROGRESS_REVIEW_20260818.md`（8/4–8/18）。
- 实时数字：Total Sent=61（8/5=40、8/14=20、8/18=1），SMTP Accepted=61，按发送日归属退信=29（8/5=14 延迟DSN 8/7 到、8/14=15/20=75%、8/18=0），退信率 47.5%，真实回复=0，OOO=0（6/25 那条在窗口外），退订=0（7/2 在窗口外），DNC=1（Record Rack 8/12）。
- 统计口径坑：8/5 批次退信在 bounce_log（按 lead 归 8/5）与 unmatched_dsn（DSN 头日期 8/7）重复出现 → 必须按邮箱去重、以 send_log 发送日期为准；gameuniverse/djhobbytoys/pulpfiction/replaytoys 4 条非窗口发送不计入。
- 叙事主线（与日志吻合）：8/4 预检拦截→8/5 发 40→8/6-8/17 主动暂停整顿（退信回流/猜测邮箱/官网真实邮箱标准）→8/18 canary 0 退信 + 库存 32。

## 运营对账（11:37，STOP DISCOVERY）
- **今日新增 V2 = 23**（ids 1056–1078，collected_at 全为 8/18），不是日报原来的 22 → 日报已修正。
- **MX Worker 已恢复** → 正式 Canonical 扫描 `_final_v2_scan_0818.py` 重算：CAMPAIGN_ELIGIBLE_V2 = **32**（ET15/CT9/MT1/PT7），含 1054（fallback 曾误报 dns_error）。FALLBACK_ONLY=0。
- **802 HOLD 已执行**：FSP 175 取消 + suppression 添加（duplicate_store_hold:701_sent）+ auto_sendable=0 → 不再进任何发送计划，未动 dedup 逻辑。DUPLICATE_HOLD=1。
- **队列 = 9**（FSP 174,176–183）；EXECUTOR 仍 PAUSED（automation-1785804421539）→ AUTO_SEND_READY=false，PRODUCTION_ISSUE=true（平台配置，非代码 bug）。
- 口径教训：收"今日新增"必须看 collected_at（last_checked_at 会被扫描批量更新，727/814/839 等 8/17 旧线索会误入）。

## 人工审核资产审计（11:57，只审不改）
- **现有审核网页 = `bd_review_server.py`**（纯 stdlib http.server，无 Flask）：`python bd_review_server.py [--port N]`，默认 8765 起自动选空闲端口；看板 `http://127.0.0.1:<port>/`，审核页 `/review`，API `/api/summary`、`/api/leads`、`/api/approve|reject|defer|manual-email|manual-a0`。
- 队列条件：`review_reason_code IS NOT NULL`（非 lead.status）；UI 对 contact_form_pool 禁用自动发送、可补邮箱/manual-A0。
- 动作写回：approve_auto→status=new/confidence=A/auto_sendable=1/review_status=approved_auto；approve_manual→approved_manual_send+manual_send_queue；reject→review_rejected；defer→review_status=deferred+next_review_at；manual-email→写回 email/evidence_*/email_source_type/email_verified_on_official_site/review_status；全部写 review_log 审计。
- 主流程路由确认（discovery/discovery_service.py:508-518）：ICP 合格+官网匹配+无邮箱无表单 → `manual_review_needed` + `review_reason_code='no_public_email_or_form'`；仅表单→`contact_form_pool`+`contact_form_only`；官网不可达→manual_review_needed+review_recovery。**与审核网页读取字段一致 → 未掉线。**

## P1.7A 审核页↔V2 兼容性审计（13:15，只读不修）
- **FSP 构建真实门槛 = V1 CAMPAIGN_ELIGIBLE，非 V2**：`bd_orchestrator.stage_pre_send` → `select_candidates_for_plan`（campaign_eligible.py:209，state IN 三州 + status 排除 + email 非空 + review_campaign_eligible==CAMPAIGN_ELIGIBLE）→ `create_plan(eligible_check=campaign_eligible_check)` → `outreach_control.build_final_plan_entries`（逐条 eligible_check + 必填 id/email/store_name/subject/body/evidence_url）。
- 发送时刻复检 `daily_session._new_outreach_gate` = V1 再判 + 排除 active manual_send_queue + 未发送；`send_one` = P0 Authorization（无授权即抛错，SMTP=0）。**FSP 构建与发送均无 MX 检查**；MX 只在 V2 库存扫描（scan_v2_inventory）上游执行。
- approve_auto（review_workflow.py:55-65）：拒绝 contact_form；hygiene 不过即拒；写 status=new/conf=A/review_status=approved_auto/auto_sendable=1，**不写 email/evidence/tz/org_key** → 不能单独获得发送资格。review_log 中 approve_auto 历史使用 = 0。
- contact_form_pool 三重保护：select_candidates SQL 排除 + broad_ready:contact_form_only + approve_auto 拒绝 → 不可能进 SMTP 链。
- manual_send_queue：当前代码**无消费方**（仅 bd_db.py:497 排除 + daily_session:250 拦截）→ approve_manual 路径是死胡同，不进自动链（安全但不流通）。
- Trace（只读）：approved_auto 仅 1 条（id=689 已 sent，双 BLOCK）；contact_form_pool 样本全 V1 BLOCK。
- **SAFETY_GAP（仅报告）**：`bd_orchestrator.stage_pre_send` 与 `daily_session._new_outreach_gate` 用 V1 而非 V2/MX——若绕过上游 V2 扫描直接用 create_plan 构建 FSP，无 MX 复核。当前 9 条队列来自 V2 池（安全），靠流程纪律而非代码强制。
- 结论：EXISTING_REVIEW_UI_V2_COMPATIBLE = partial（审核页本身可用且不与新链冲突；建议恢复 /review 优先处理 467+72 库存，但补邮箱/批准后的 lead 需经 V2 扫描确认 MX 后才可入队）。
- Smoke Test（8/18 实测）：REVIEW_UI_STARTS=true，/、/review、/api/summary 全 200；DB counts：manual_review_needed=467、contact_form_pool=72、review 队列 pending≈517。结论：现成可用，只需重新启动即可继续使用，不做开发。

## P1.7B 人工池机器优先恢复（14:17，样本 50 条，零写库）
- 池对账：manual_review_needed=467（website_lookup_required 244 / 空 90 / CONTACT_ROLE_UNCERTAIN 83 / WEAK_EVIDENCE 45 / 其他 5）；**377 无邮箱、350 无官网**；已发送 51、重复域 2、重复 org 1、suppressed 0；ICP route A=293/U=82/B=12。contact_form_pool=72。
- 有官网+未发送+非重复 = 64 条 → 取前 50 条跑机器恢复（严格第一方邮箱验证：域名匹配官网 + 页面真实出现，禁止猜测）。
- **结果：0/50 恢复，0 拒绝，50 留人工。** 根因（环境实测）：35/50 站点直连+代理均不可达（SSL EOF/握手超时）；15/50 可达但全是 JS 渲染 SPA/重定向壳（thirdplanet 跳转页、toptentoys 纯 CSS、maximumcomics JS 引导），静态 HTML 零邮箱；WebSearch 也搜不到这些店官方邮箱索引。34/50 带 info@ 猜测邮箱（CONTACT_ROLE_UNCERTAIN）——未在页面出现 → 不采信（正确防退信）。
- 验证：零写库（manual_decision 无 machine_* 记录，池仍 467）。
- 结论：本环境静态机器恢复该池价值≈0；467 不值得继续大规模静态 Machine-First。建议：/review 人工只处理"有官网+有邮箱线索"子集（CONTACT_ROLE_UNCERTAIN 83 + WEAK_EVIDENCE 45 ≈ 128 条），其余（无官网 350/已发 51/重复 3）自动降级，无需人工逐条。JS 渲染站需 browser_verifier（Playwright）才能验证，本轮未启用（SYSTEM_FREEZE）。

## 人工审核网页恢复运行（15:26）
- 已启动 `bd_review_server.py --port 8765`（Start-Process 独立进程，PID 2948，持续运行不随会话退出），监听 127.0.0.1:8765（未被占用，用指定端口）。
- 验证：GET / 、/review、/api/summary 全 200；DB 连接正常：manual_review_needed=467、contact_form_pool=72、队列 pending=515。
- REVIEW_URL = http://127.0.0.1:8765/review；日志 output/review_server_runtime.log(.err)。

## A0↔V2 直发绕过审计（16:53，只读）
- **结论：无直发绕过。** send_one 的 P0 门（bd_sender.py:464+）：非 dry-run 生产发送必须带真实 final_plan_entry_id(int)+有效 authorization_id，否则 failed/blocked，不连 SMTP；test_mode 仅发测试邮箱。
- 旧 A0 直调 send_one 的三处均为死代码/CLI，且全部会被 P0 门拦截：auto_replenish_loop.py:206（**孤儿零引用**）、bd_main.py:143（CLI test-send，test_lead 无 FSP id）、daily_operator_auto.py（**无 send_one**，step_send_batches 669 行委托 daily_session.py --live --force = canonical 链；_create_connection 仅用于 preflight 连通性检查）。
- 审核侧：bd_review_server（api_manual_email/api_manual_a0）与 review_workflow（approve_auto/approve_manual/reject/defer）只写字段+manual_send_queue（无消费方），无发送路径。
- A0_ROLE=遗留选择器/辅助字段（get_sendable_leads/confidence=A/auto_sendable 不再有资格权）；APPROVAL_ROLE=只写字段不入链；V2_ROLE=上游库存门控（MX+证据+新鲜度）。兼容 = A0 数据经 V2 扫描（MX）后即可正常入链，无需改代码。

## 恢复 OPERATE THE SYSTEM（17:00）
- 审计项关闭；系统保持 freeze，仅运营。Canonical 扫描（Worker 正常）：**V2 = 33**（新增 id=19 Building Blocks Toy Store 此前被 worker 抖动误报），≥30 ✅。
- 今日发送 1（Canary），退信 0，回复 0。今日 3 条 bounce_log（08:35 同步）为 8/14 已知退信重复扫描（unresolved、无诊断）——观察项：result_recovery_sync/bounce_pipeline 恢复扫描疑似缺 (lead,email) 去重，仅记录不修。
- 执行器 automation-1785804421539 仍 PAUSED（ExecutionHost 未验证）→ 今晚 9 条（FSP 174,176–183）需用户决策：取消暂停 or 授权窗口手动正式链发送。
- 注：前台 Bash 跑大输出脚本曾静默失败（exit 1 无输出），后台 run_in_background + TaskOutput 正常——后续大脚本用后台方式。

## 地理优先级/三州遗留门禁审计（17:13，只读）
- **城市队列**：retail_city_queue=19（TN 1-3 优先、AR 10-11、KY 20-21；非三州 Metro 全 pri=30）。状态用大写枚举：CONTACT_ENRICHMENT_IN_PROGRESS=2（Nashville/Memphis）、QUEUED=4、PENDING=12、search_matrix_exhausted=1。无 rural/旅游小镇条目 → SMALL_CITY_FIRST=partial。
- **PRODUCTION_CITY_SELECTOR = retail_city_queue**（bd_orchestrator:358 activate_next_city + discovery_service 消费）；city_selector.py 是遗留孤儿链（auto_collector↔collection_pipeline 互引，无人生产引用）。
- **州门禁分类**：V2 层**无**州门（state-agnostic ✓）；FSP 层有硬门（campaign_eligible.py:210 select_candidates_for_plan 默认 states=("TN","AR","KY")，bd_orchestrator.stage_pre_send:104 未覆盖 → orchestrator 自动路径非三州进不了 FSP）；REVIEW 层有门（bd_review_server.py:380 other_state_retail_blocked）；DISCOVERY 层有门（web_directory.py:30-32 仅三州目录、inventory_monitor:118/213、bd_orchestrator:433 富化 lane 仅三州）；REPORT_ONLY（dashboard/final_audit）。
- **C 实证**：8 条 V2 全 PASS（3 州 2 条 + 非 3 州 6 条），非三州经 select_candidates_for_plan = NO；当前 9 条队列含 7 条非三州（靠 _prepare_remaining_10.py 手动 create_plan 绕过）。NON_3_STATE_V2_CAN_ENTER_FSP = false（自动路径）。
- **冲突点**：州硬门残留在 FSP/REVIEW/DISCOVERY 层，与"州不再作为邮箱资格硬门禁 + 全国扩州"策略冲突。LAYERING_CONCEPT_COMPATIBLE=false（仅报告，不修）。

## GEO STRATEGY ALIGNMENT（17:31，只读，最小变更规划）
- 新策略：state-by-state deep discovery（首测 NY Upstate-first），州不参与邮箱资格。
- 现状：retail_city_queue 的 activate_next_city/active_city **无 state 参数**（纯 priority）；seed_default_queue 用 outreach_control.DEFAULT_RETAIL_CITIES（无 NY）；城市完成机制可复用（all_city_completion_conditions_met + 状态机 + complete_if_exhausted），**无州级完成标记**（州完成=派生 SQL 查询，无需新表）；city_discovery_reports 是 per-city 报表（0 行，无 state 字段）。
- 必须改的 6 处州硬门：campaign_eligible.py:210（select_candidates_for_plan 默认三州）+ bd_orchestrator.py:104/313/433；bd_review_server.py:380（other_state_retail_blocked）；web_directory.py:30-32（目录 URL 仅三州）；inventory_monitor_executor.py:63/118/213；fb_fallback_recovery.py:41/188（+facebook_enrichment 3 处，可选）。
- 不动：icp_profile 区域标注、dashboard REPORT_ONLY、daily_operator_auto 遗留默认、V2 门、canonical 链（bd_sender/daily_session/broad_ready）。
- MINIMAL_CHANGE_FILES=7：campaign_eligible.py、bd_orchestrator.py、bd_review_server.py、discovery/providers/web_directory.py、inventory_monitor_executor.py、retail_city_queue.py（+outreach_control.py 城市数据）、fb_fallback_recovery.py（可选）。NEW_SCHEMA=false、NEW_PIPELINE=false。

## P1.7C State Scope 最小兼容性修复（17:36，已实施+回归 12/12 PASS）
- **修改 5 文件（仅州门，其余逻辑未动）**：
  1. campaign_eligible.py `select_candidates_for_plan(states=None)` → 默认全州；传元组仍按州限定（旧调用兼容）。
  2. bd_orchestrator.py `_count_broad_ready_pool`（313）与官网富化 lane（433）移除 `state IN ('TN','AR','KY')`。
  3. bd_review_server.py 移除 hygiene 的 `other_state_retail_blocked`（州不再参与审核/邮箱资格）。
  4. inventory_monitor_executor.py `ALLOWED_STATES=frozenset()`（空=全州）+ count_sendable/retail lane 移除州过滤。
  5. retail_city_queue.py `active_city(conn,state=None)` / `activate_next_city(conn,state=None)`（state 过滤可选，None=旧行为）+ `seed_state_cities` + `NY_FIRST_ROUND_CITIES`（17 城 Upstate-first：Ithaca/Saratoga/Cooperstown/Lake Placid/Oneonta/New Paltz/Corning/Glens Falls/Plattsburgh/Watertown/Utica/Binghamton/Poughkeepsie/Syracuse/Albany/Rochester/Buffalo，排除 NYC/Long Island）。
- 未改：V2 门、Sender/链条、A0 分层、web_directory（未配置=优雅降级不阻断）、FB lane（不在 canonical 链，未动）、无新表/无新 pipeline。
- NY 已 seed 生产队列（17 条 pending，TOTAL_QUEUE 19→36）；审核服务器已重启加载修复（PID 33604，/ /review /api/summary 全 200）。
- 回归（_p17c_regression_0818.py，不发送）：NY V2→V1/V2 PASS、FL/PA/WA 进 FSP 候选、三州正常、guessed_email/suppressed/bounced/contact_form_only 全部 BLOCK、activate_next_city(state='NY')×5 全 NY、state=None 旧行为正常 → **12/12 PASS**。

## P1.7C FINAL CHECK（17:48，只读）— 生产接线缺口
- 唯一生产调用点 = `bd_orchestrator.py:361`：`activate_next_city(city_conn)` **未传 state** → PRODUCTION_PASSES_STATE_SCOPE=false。
- 队列实际活跃 = Nashville TN（CONTACT_ENRICHMENT_IN_PROGRESS pri1）→ 今天启动 Discovery 下一座城市 = **Nashville TN**（非 NY）。
- NY 17 城因 priority 1-17 恰好排在 pending 前部（Ithaca pri1 紧随 Nashville），是偶然排序而非机制保证；无 state 作用域时不保证"NY 完成前不出州"。
- **缺口（未修）**：需为 orchestrator 提供 active-state 来源（如 BD_ACTIVE_STATE 配置/env）并传入 activate_next_city(state=...)，或在 discovery stage 按 FIRST_TEST_STATE=NY 显式接线。STATE_SCOPE_CAPABILITY_EXISTS=true。

## P1.7C FINAL WIRING（17:51，完成，8/8 PASS）— 生产 active state 接线
- `bd_orchestrator.py` 唯一改动：新增 `US_STATE_CODES` + `_resolve_active_discovery_state()`（读 system_config 键 `active_discovery_state`，校验 2 位州码，返回 (state, err)，err∈{ACTIVE_STATE_NOT_SET, ACTIVE_STATE_INVALID}）；`stage_inventory` 改为：fail-closed 解析 → `seed_default_queue` → `activate_next_city(city_conn, state=active_state)`；state 缺失/非法 → finish_job_run(stop_reason=ACTIVE_STATE_*) 且**不调用 state=None**；NY 无待处理城市 → RuntimeError → `ACTIVE_STATE_EXHAUSTED:NY` 停止等人工，不自动切换。Send/Bounce/Reply/Dashboard 链不受影响。
- `system_config.active_discovery_state = NY`（经 set_config 写入）。
- 回归（_p17c_wiring_regression_0818.py，不发送）：helper 解析 NY ✓；生产路径 5 连 NY（Ithaca→Saratoga→Cooperstown→Lake Placid→Oneonta）✓；Nashville(TN, enrichment 中) 不被 NY scope 消费 ✓；INVALID→ACTIVE_STATE_INVALID ✓；缺失→ACTIVE_STATE_NOT_SET ✓；production 下一城=Ithaca NY（17 城全 pending）✓。
- NEXT_PRODUCTION_CITY=Ithaca, NY。NY_ONLY_UNTIL_EXHAUSTED=true、GLOBAL_FALLBACK_DISABLED=true。地理系统开发到此停止。

## P1.7D NY 受控运营批次（18:02，生产路径实测）
- 生产流程按 state=NY 正常运转：activate_next_city(state='NY') → **Ithaca**（id=20, active）→ run_places_batch → **configuration_blocked**（provider=google_places, error=provider_not_configured, results=0, leads_created=0）→ postprocess 空。fail-closed 正确，队列 checkpoint（provider_errors=1）。
- **根因（只报告）**：NY 无可用 provider——google_places 缺 GOOGLE_MAPS_API_KEY（.env 无）、web_directory 仅三州、browser_maps 需人工 Playwright。不开发新抓取器（遵守约束）。

## TONIGHT V2 SEND PRECHECK（18:10，未发送 — 硬 blocker）
- **执行器 automation-1785804421539 仍 PAUSED**（ExecutionHost未验证）→ 今晚 22:00 ET 窗口不会自动飞出。这是唯一硬 blocker。
- 其余全部 PASS：V2 unsent=33（Canonical worker，全 mx=ok，ET15/CT10/MT1/PT7）；manual_pause=false；standing_authorization=true；risk_gate_status=clear；scheduler_enabled=true；SMTP 465 / IMAP 993 TLS 握手正常（smtp/imap.exmail.qq.com）；MX Worker ok；V5 template SHA ccb51505 匹配；BD_TEST_MODE=false；execution_mode=inventory_recovery（can_send_live 四门全过）。
- 9 条 planned FSP（174,176–183）：lead 727/814/815/839/883/923/1018/1053/1054 全 V2 PASS、官方邮箱、无 suppression/无已发送/无重复 org。
- 结论：PRECHECK_PASS=false（仅因执行器 PAUSED）；未创建新授权/FSP、未发送任何邮件。delayed DSN check 由既有 automation-1786002601925（每日 08:45 Result Recovery Sync，ACTIVE）覆盖。
- 结果：RAW_STORES_FOUND=0、NEW_UNIQUE_LEADS=0、NEW_V2=0、NY_V2_YIELD=0。DB 现存 22 条 NY 线索为历史数据（11 sent / 4 contact_form_pool / 5 manual_review，多在 NYC/Brooklyn），非本批次产出。
- NEXT_PRODUCTION_CITY=Saratoga Springs, NY；NY_STATE_EXHAUSTED=false。队列 17 城完整（Ithaca active 待 provider 可用后继续）。

## P1.7D1 browser_maps 兼容性审计（18:06，只读）
- 资产：`discovery/providers/browser_maps.py`（SearchProvider 接口，FILE/DIRECT 双模式）+ `browser_maps_scraper.py`（CLI）+ RUNBOOK。已在 base.py load_provider 注册（browser_maps），但 env DISCOVERY_PROVIDER 未设 → 生产默认 google_places，未引用。
- 输入：search_places(query, city, state) 任意 city/state（Ithaca/NY ✓），query 来自现有 RETAIL_QUERY_FAMILIES ✓。输出：ProviderPage/PlaceSearchResult → DiscoveryService 原生消费 → 现有 staging/normalization/dedup/website/email/V2 全链可复用。无旁路（不写 leads/A0/邮箱/V2/FSP/send）。
- 运行要求：无 API key、无 Google 登录、无人工点击（headless+滚动+captcha 检测）；**Playwright 未安装**（需 pip install playwright + playwright install chromium）；Google Maps 需网络可达（本环境需 Astrill，注意 Google 共享 IP 429 限流）。FILE 模式=用户先跑 scraper 输出 JSON；DIRECT 模式=provider 内联调用。
- Ithaca 可行性：能力就绪，**零代码改动**启用路径 = 用户跑 `browser_maps_scraper.py --query "toy store Ithaca NY" --city Ithaca --state NY --output data/browser_maps_cache/xxx.json` → 设 DISCOVERY_PROVIDER=browser_maps（或 BROWSER_MAPS_JSON_FILE）→ 现有 stage_inventory 自动消费。

## P1.7E 生产执行器权威审计（8/19 08:54，只读）— 重大真相
- **Windows Task Scheduler 真实存在且启用**（schtasks/sc 被安全策略禁用，改用 PowerShell Get-ScheduledTask 查到）：
  - `RoktRazo-BD-Daily-Outreach`：每天 09:00 `daily_operator_auto.py --live --target-count 20 --allow-topup --stop-on-risk`，STATE=Ready，LAST=8/18 09:00 RESULT=0，NEXT=8/19 09:00
  - `RoktRazo-BD-Morning` 08:30（bd_orchestrator --stage status --dry-run）；`RoktRazo-BD-Inventory-Recovery` 15:00；`RoktRazo-BD-Inbox-Morning` 08:30 / `RoktRazo-BD-PostSend-Poll` 13:10（inbox_polling_monitor，**LAST RESULT=1 异常**）；`Roktandrazo_Daily_Outreach` 10:00（旧 daily_runner.py --dry-run）
- **RoktAndRazoBDExecutionHost Windows 服务不存在**（SERVICE_NOT_FOUND）— 记忆中的"服务权威"是误记/已删
- bd_orchestrator.py 头注释：**"Scheduler authority: Windows Task Scheduler only. WorkBuddy Automations: DISABLED for production sends."** → 代码声明的权威 = Windows Task Scheduler，WorkBuddy Automation 明确禁用
- **daily_operator_auto.py 窗口常量 = 23:00–24:00**（WINDOW_START_HOUR=23.0），而 Windows Task 9:00 触发 → **触发时间与窗口错配**：9:00 运行时 is_in_send_window()=False → 走 outside_send_window 分支 → **不发送**（RESULT=0 只是正常退出）
- 夜间 21:30–23:59 无任何 BD 相关 Task 触发（仅系统任务）
- send_log 实证：8/5 批次 18:47、8/14 批次 09:28、8/18 canary 09:21 **全部不在 23:00–24:00 窗口内** → 历史所有发送都是人工/一次性批次（override），**从未有真正的自动窗口发送**
- 昨晚 0 attempted 根因 = 调度权威错配：Windows Task（9:00）与发送窗口（23:00–24:00）不匹配 + WorkBuddy Automation（22:00=ET10:00 正确窗口）被代码声明禁用且 PAUSED + ExecutionHost 服务不存在 → 22:00/23:00 窗口无任何执行者
- 结论：DAILY_MANUAL_CONFIRMATION_REQUIRED=true（当前实然），但设计应为 false；ONE_TIME_FIX_TARGET=统一调度权威（把 Windows Task 触发时间对齐窗口，或恢复正确窗口的执行者），仅报告未修改













