# 2026-08-25

## P2.3F — DNS freshness self-bootstrap bug fix (production)
- **Bug**: `preflight_gate.check_dns_freshness` 在缓存缺失/超龄时现场 `query_mx` 成功，仍把域标 `stale=True`，导致首次运行只能 warm cache、第二次才能发送（自举缺陷，正是 P2.3E 发现的 PRODUCTION_DNS_REFRESH_GAP 根因）。
- **Fix (仅改 `preflight_gate.py:check_dns_freshness`)**: 冷/超龄分支——live `query_mx` 返回 `ok` → 立即 `stale=False/fresh=True`（同一次运行 PASS）；返回 dns_error/no_mail_route/nxdomain/null_mx/timeout/unauthorized/invalid_mx → `stale=True`（FAIL CLOSED）。fresh 缓存路径不变。未改 V2/Sender/manual_pause/Scheduler/24h TTL/数据库 schema。
- **回归测试** `output/_test_p2_3f.py`（临时 DB 副本，不发信）: 8/8 PASS（含 TEST_2 无缓存 live ok 同跑 PASS、TEST_3 超龄缓存 live ok 同跑 PASS、TEST_4/5 FAIL CLOSED、TEST_6 Fallout 真实 no_mail_route BLOCK、TEST_7 真实 ok PASS、TEST_8 run_preflight 冷缓存首跑 PASS）。EXIT_CODE=0。
- P2.3B 链完整：`daily_session.execute_final_send_plan` 仍调 `run_preflight`（PRE_AUTH L106 / POST_AUTH L139）。`manual_pause` 仍为 true（未触碰）。
- 结论：今晚若解除 manual_pause，DNS Preflight 不再因自举缺陷阻断；但仍需真实 live MX=ok 的候选。未做发送。
