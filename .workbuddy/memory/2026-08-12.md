# 2026-08-12

## P0 ICP + Lead Recovery (17:28-18:10 CST)
产出: ICP_V1.md + icp_profile.py + P0_ICP_RECOVERY_DELTA_20260812.md + output/p0_icp_recovery_metrics.json
备份: backup/p0_icp_recovery_20260812_172846/ (SHA da0a6a9a)

### Inventory 0 产出根因（多因叠加）
1. **A-决定性**: 发现层依赖 Google Places/SerpAPI，.env 无 key → configuration_blocked，0 请求 0 结果
2. **D-直接原因**: 官网扫描能找到的邮箱几乎全部 previously_sent（08-12 inventory_lane 14 条提交全被拦）
3. **F-空转**: 候选 SQL 无已处理排除，同批 35 条被 5 loops×每天重复扫描
4. **C-次要**: curl 无 --compressed，gzip 触发 UnicodeDecodeError 误判网络失败
5. **E-毒化**: 14 条伪邮箱 lead（@2x.jpg/sentry/www.）占用名额

### 修复（正式模块最小改动）
- inventory_monitor_executor.py: curl --compressed + binary 解码; extract_emails 接入 email_hygiene 过滤伪邮箱
- bd_orchestrator.py: 候选 SQL 排除已提交(manual_email_submission inventory_lane); evidence_url 用实际页面
- leads 表: 清空 12 条真伪邮箱（保留 2 审计样本）; 新增 icp_route/icp_priority/icp_reason/icp_classified_at/icp_version 并回填

### 结果
- ICP V1: Route A(零售分销)/B(定制) + P1/P2/P3 + Negative ICP
- 本轮入库 11 条线索（WebSearch 真实采集，非伪造）: 4 条 Broad Ready（Village Tinker/Stoney's/Moonlite/Legit MTG）, 5 条 new 有邮箱, 1 Negative(Record Rack)
- Recovery Gate 1 ✅(>0) / Gate 2 部分(4/10 Ready) / Gate 3 ❌(30/日)
- 30/日 不可达真实原因: 三州存量有限+已发送覆盖+官网邮箱公开率低+缺 API key
- 回归测试 34 passed + 32 subtests; CUSTOMER_SMTP_CONNECTIONS=0

### 发现的问题
1. 根目录出现 5 个新日期脚本（_preflight_20260810/11 等，自动任务产生）→ 已归档 _archived_scripts/2026-08-12/
2. broad_ready shared_domain_org_history 把 yahoo/hotmail 免费邮箱当组织历史拦截（与 ICP P3 冲突，需复核）
3. live inventory 官网扫描 25min 无产出（扫描过慢本身是问题，需限时）
4. 需给 .env 配 Places/SerpAPI key 或新增 WebSearch 采集路径
